# 任务002: 创建 Settings Context 和 Hook

**状态**: Todo
**优先级**: High
**预计时间**: 4h

**占用者**: -
**锁定时间**: -
**锁定超时**: 8 分钟（超时后自动释放）

## 任务描述

创建全局设置管理机制，使用 React Context 和自定义 Hook

## 依赖任务

- [ ] 任务001 (状态: Todo) - 必须先完成

**说明**: 只有所有标记的依赖任务状态为 Done 后，此任务才能开始执行。

## 验收标准


- [ ] SettingsContext 和 useSettings Hook 可正常导入使用
- [ ] 组件可通过 useSettings 获取和更新设置
- [ ] 设置变更自动保存到后端
- [ ] 设置加载失败有降级方案（使用 localStorage）
- [ ] 设置重置功能正常工作

## 实施步骤

1. 创建 src/contexts/SettingsContext.tsx
2. 定义 SettingsProvider 组件
3. 创建 src/hooks/useSettings.ts
4. 实现设置状态管理（加载、更新、保存）
5. 实现设置变更监听机制
6. 添加设置重置功能
7. 添加设置持久化逻辑（localStorage + Tauri）
8. 导出 useSettings Hook
9. 在 App.tsx 中包裹 SettingsProvider

## 阻塞原因

**如果状态为 Blocked，在此记录阻塞原因**：
- 等待任务XXX完成
- 等待外部资源（API密钥、审批等）
- 技术问题（需要解决XXX）

## 并行提示

- 需等待 任务001 完成

## 备注

[执行过程中的备注]

## 完成记录

- 开始时间: _______
- 完成时间: _______
- 耗时: _______