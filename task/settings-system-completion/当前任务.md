# 任务001: 实现 Tauri 后端存储命令

**状态**: Todo
**优先级**: High
**预计时间**: 3h

**占用者**: -
**锁定时间**: -
**锁定超时**: 6 分钟（超时后自动释放）

## 任务描述

在 Rust 后端实现设置的加载和保存命令，支持文件系统持久化

## 依赖任务

- 无依赖

**说明**: 只有所有标记的依赖任务状态为 Done 后，此任务才能开始执行。

## 验收标准


- [ ] load_settings 命令可正常调用并返回 AppSettings
- [ ] save_settings 命令可正常调用并写入文件
- [ ] 设置文件保存在 ~/.pi/session-manager/settings.json
- [ ] 错误情况有清晰的错误消息
- [ ] 单元测试通过

## 实施步骤

1. 在 src-tauri/src/commands.rs 中添加 load_settings 命令
2. 在 src-tauri/src/commands.rs 中添加 save_settings 命令
3. 定义 SettingsFile 结构体（用于 JSON 序列化）
4. 实现从 ~/.pi/session-manager/settings.json 读取设置
5. 实现设置写入到文件系统（自动创建目录）
6. 添加错误处理和日志记录
7. 在 src-tauri/src/lib.rs 中注册新命令
8. 编写单元测试验证读写功能

## 阻塞原因

**如果状态为 Blocked，在此记录阻塞原因**：
- 等待任务XXX完成
- 等待外部资源（API密钥、审批等）
- 技术问题（需要解决XXX）

## 并行提示

- 无依赖，可立即开始

## 备注

[执行过程中的备注]

## 完成记录

- 开始时间: _______
- 完成时间: _______
- 耗时: _______