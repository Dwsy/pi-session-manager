# 任务003: 重构 SettingsPanel 使用 Context

**状态**: Todo
**优先级**: High
**预计时间**: 2h

**占用者**: -
**锁定时间**: -
**锁定超时**: 4 分钟（超时后自动释放）

## 任务描述

将 SettingsPanel 从独立状态管理改为使用全局 Settings Context

## 依赖任务

- [ ] 任务002 (状态: Todo) - 必须先完成

**说明**: 只有所有标记的依赖任务状态为 Done 后，此任务才能开始执行。

## 验收标准


- [ ] SettingsPanel 使用全局设置状态
- [ ] 设置保存到文件系统（不再仅 localStorage）
- [ ] 设置变更在组件间同步
- [ ] UI 交互正常无变化

## 实施步骤

1. 在 SettingsPanel 中导入 useSettings Hook
2. 移除 SettingsPanel 内部的 settings state
3. 移除 loadSettings 和 saveSettings 函数
4. 使用 useSettings 提供的 settings 和 updateSetting
5. 简化重置逻辑（调用 useSettings 的 reset）
6. 测试设置加载和保存功能
7. 验证设置持久化到文件系统

## 阻塞原因

**如果状态为 Blocked，在此记录阻塞原因**：
- 等待任务XXX完成
- 等待外部资源（API密钥、审批等）
- 技术问题（需要解决XXX）

## 并行提示

- 需等待 任务002 完成

## 备注

[执行过程中的备注]

## 完成记录

- 开始时间: _______
- 完成时间: _______
- 耗时: _______