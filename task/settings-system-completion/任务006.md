# 任务006: 修复 sidebarWidth 数据一致性

**状态**: Todo
**优先级**: High
**预计时间**: 1.5h

**占用者**: -
**锁定时间**: -
**锁定超时**: 2 分钟（超时后自动释放）

## 任务描述

统一 SessionViewer 和 SettingsPanel 中的 sidebarWidth 管理

## 依赖任务

- [ ] 任务002 (状态: Todo) - 必须先完成

**说明**: 只有所有标记的依赖任务状态为 Done 后，此任务才能开始执行。

## 验收标准


- [ ] sidebarWidth 只有一个数据源
- [ ] 拖拽调整和滑块调整同步
- [ ] 设置持久化到文件系统
- [ ] 侧边栏宽度在所有组件中一致

## 实施步骤

1. 移除 SessionViewer 中独立的 localStorage key
2. SessionViewer 使用 useSettings Hook 获取 sidebarWidth
3. 统一默认值为 320px
4. SessionViewer 拖拽调整时调用 updateSetting
5. SettingsPanel 中添加 sidebarWidth 滑块控制
6. 测试拖拽调整和滑块调整的同步
7. 验证设置持久化

## 阻塞原因

**如果状态为 Blocked，在此记录阻塞原因**：
- 等待任务XXX完成
- 等待外部资源（API密钥、审批等）
- 技术问题（需要解决XXX）

## 并行提示

- 需等待 任务002 完成

## 备注

[执行过程中的备注]

## 完成记录

- 开始时间: _______
- 完成时间: _______
- 耗时: _______