# 任务014: 统一 Pi Config 处理

**状态**: Todo
**优先级**: Medium
**预计时间**: 2h

**占用者**: -
**锁定时间**: -
**锁定超时**: 4 分钟（超时后自动释放）

## 任务描述

将 Pi Config 纳入 AppSettings 统一管理

## 依赖任务

- [ ] 任务002 (状态: Todo) - 必须先完成

**说明**: 只有所有标记的依赖任务状态为 Done 后，此任务才能开始执行。

## 验收标准


- [ ] Pi Config 在 AppSettings 中管理
- [ ] Skills/Prompts 切换正常
- [ ] settings.json 正确写入
- [ ] 所有设置统一持久化
- [ ] 向后兼容旧数据

## 实施步骤

1. 在 AppSettings 中添加 piConfig 字段
2. 定义 PiConfigSettings 类型
3. 修改 PiConfigSettings 组件使用 useSettings
4. 移除 PiConfigSettings 独立的 Tauri 调用
5. 将 Pi Config 保存逻辑集成到 Context
6. 测试 Skills/Prompts 启用切换
7. 验证 settings.json 写入
8. 确保向后兼容

## 阻塞原因

**如果状态为 Blocked，在此记录阻塞原因**：
- 等待任务XXX完成
- 等待外部资源（API密钥、审批等）
- 技术问题（需要解决XXX）

## 并行提示

- 需等待 任务002 完成

## 备注

[执行过程中的备注]

## 完成记录

- 开始时间: _______
- 完成时间: _______
- 耗时: _______