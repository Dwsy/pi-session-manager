# 任务005: 迁移核心组件使用语义化颜色

**状态**: Todo
**优先级**: High
**预计时间**: 60m

**占用者**: -
**锁定时间**: -
**锁定超时**: 120 分钟（超时后自动释放）

## 任务描述

将核心组件中的硬编码颜色替换为 Tailwind 语义化颜色，使其响应主题切换

## 依赖任务

- [ ] 任务004 (状态: Todo) - 必须先完成

**说明**: 只有所有标记的依赖任务状态为 Done 后，此任务才能开始执行。

## 验收标准


- [ ] 所有核心组件不再使用硬编码颜色
- [ ] 组件使用 bg-background, text-foreground 等语义化类名
- [ ] 组件在 light 和 dark 主题下都正常显示

## 实施步骤

1. 搜索项目中所有硬编码的颜色值（如 #1a1b26, #2c2d3b）
2. 替换 App.tsx 中的硬编码颜色为 bg-background, text-foreground 等
3. 替换 SessionList.tsx 中的硬编码颜色
4. 替换 SessionViewer.tsx 中的硬编码颜色
5. 替换 SearchPanel.tsx 中的硬编码颜色
6. 替换 SettingsPanel.tsx 中的硬编码颜色
7. 替换 ProjectList.tsx 中的硬编码颜色
8. 替换 Dashboard.tsx 中的硬编码颜色

## 阻塞原因

**如果状态为 Blocked，在此记录阻塞原因**：
- 等待任务XXX完成
- 等待外部资源（API密钥、审批等）
- 技术问题（需要解决XXX）

## 并行提示

- 需等待 任务004 完成

## 备注

[执行过程中的备注]

## 完成记录

- 开始时间: _______
- 完成时间: _______
- 耗时: _______