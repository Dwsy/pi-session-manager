# @tintinweb/pi-subagents > 「よりれ」—— ---## Architecture（！）```┌─────────────────────────────────────────────────────────┐│ System │├─────────────────────────────────────────────────────────┤│ ││ ① @tintinweb/pi-subagents (Pi ) ││ ┌─────────────────────────────────────────────┐ ││ │ ：Pi agent │ ││ │ Feature： │ ││ │ ：pi.registerMessageRenderer() │ ││ │ ：.output (JSONL) + .meta.json │ ││ └─────────────────────────────────────────────┘ ││ ││ ② Pi Session Manager (Project) ││ ┌─────────────────────────────────────────────┐ ││ │ ： Tauri │ ││ │ Feature：/ Pi Session │ ││ │ ：React Component │ ││ │ ： ~/.pi/agent/sessions/ │ ││ └─────────────────────────────────────────────┘ ││ ││ = Pi Session Manager ││ @tintinweb File ││ │└──────────────────────────────────────────────────────────┘```---## Complete### ✅ Phase 1: meta.json ****：- `turns` （Rust + TypeScript）- SQLite `turns` - UI Display turns **Verify**：- 12/12 Test- （ turns 0）**State**：✅ Complete---### ⚠️ Phase 2: JSONL （Verify）**@tintinweb .output **：```json{"isSidechain":true,"agentId":"abc123","type":"user","message":{"role":"user","content":"Find auth files"},"timestamp":"2026-03-26T10:00:00.000Z","cwd":"/project"}{"isSidechain":true,"agentId":"abc123","type":"assistant","message":{"role":"assistant","content":"...","toolCalls":[...]},"timestamp":"2026-03-26T10:00:05.000Z","cwd":"/project"}{"isSidechain":true,"agentId":"abc123","type":"toolResult","message":{"role":"toolResult","toolCallId":"call_1","content":"..."},"timestamp":"2026-03-26T10:00:10.000Z","cwd":"/project"}```** SessionEntry Type**：```typescriptinterface SessionEntry { type: string id: string // ⚠️ @tintinweb parentId?: string timestamp: DateTime<Utc> // ⚠️ ISO string message?: Message // ...}```**Issue**：1. @tintinweb entry ** `id` **2. `timestamp` 3. `message` Verify**** (`src/utils/session.ts`)：```typescriptexport function parseSessionEntries(jsonlContent: string): SessionEntry[] { const entries: SessionEntry[] = [] for (const line of jsonlContent.split('\n')) { if (!line.trim()) continue try { const entry = JSON.parse(line) // id，？ entries.push(entry) } catch { /* skip */ } } return entries}```**Verify**：- [ ] @tintinweb Test JSONL File- [ ] Verify `parseSessionEntries` - [ ] Verify `SubagentModal` ---## @tintinweb TUI （）****（ Pi agent ）：```typescript// 1. Messagepi.registerMessageRenderer<NotificationDetails>( "subagent-notification", (message, { expanded }, theme) => { // return new Text(renderedLines, 0, 0) })// 2. Messagepi.sendMessage<NotificationDetails>({ customType: "subagent-notification", content: "<task-notification>...</task-notification>", display: true, details: { id: "abc123", description: "Find auth files", status: "completed", toolUses: 3, turnCount: 5, totalTokens: 12400, durationMs: 4500, outputFile: "/tmp/.../agent.output", resultPreview: "Found 5 files...", },}, { deliverAs: "followUp", triggerTurn: true })```****：```✓ Find auth files completed ⟳5 · 3 tool uses · 12.4k token · 4.5s ⎿ Found 5 files related to authentication... transcript: /tmp/.../agent.output```---## ****（Tauri + React）：```typescript// ToolCallList.tsxcase 'subagent': return ( <SubagentToolCall arguments={args} details={result?.message?.details} output={output} entryId={entryId} /> )```**SubagentToolCall **：```tsx<div className="subagent-tool-call"> <div className="subagent-header"> ✓ Subagent [Parallel × 3] </div> <div className="subagent-results-grid"> {results.map(result => ( <ResultCard result={result} onClick={...} /> ))} </div></div>```**ResultCard Display**：```✓ Explore anthropic/claude-sonnet-4🕐 4.5s 🧠 6.9k 🔧 3 tools 🤖 5 turnsFound 5 files related to authentication...```---## Analysis| Feature | @tintinweb | Project | ||------|-----------|--------|--------|| **meta.json ** | ✅ | ✅ | ✅ Complete || **turns ** | ✅ `usage.turns` | ✅ | ✅ Complete || **JSONL ** | ✅ | ⚠️ Verify | ⚠️ Test || **outputFile Display** | ✅ transcript | ⚠️ Type，UI Display | ⚠️ Implementation || **custom_message ** | ✅ `subagent-notification` | ✅ `CustomMessage` Component | ⚠️ Verify |---## （）### A：Verify（）1. @tintinweb/pi-subagents2. 3. Verify Pi Session Manager Display****：30 ****：### B：TestData1. @tintinweb .output .meta.json2. `~/.pi/agent/sessions/test-session/subagent-artifacts/`3. Verify****：1 ****：### C：Verify1. `parseSubagentOutputEntry()` 2. @tintinweb `SessionEntry`3. Test****：2-3 ****：---## ✅ **Phase 1 Complete**：meta.json ，turns ⚠️ **JSONL Verify**：，🎯 ****： A B Verify，---## File### Complete```src-tauri/src/models.rs ✅src-tauri/src/subagent.rs ✅src-tauri/src/sqlite_cache/subagent_meta.rs ✅src-tauri/src/sqlite_cache/bootstrap.rs ✅src-tauri/src/sqlite_cache/schema.rs ✅src-tauri/src/sqlite_cache/migrations.rs ✅src/types.ts ✅src/components/SubagentToolCall.tsx ✅src/components/SubagentModal.tsx ✅src-tauri/tests/subagent_cost_test.rs ✅src-tauri/tests/tintinweb_compat_test.rs ✅ ()```### TestState```Rust Test：12/12 ✅TypeScript ：Error ✅Rust ：Success ✅```---> 「はにかず」—— @tintinweb/pi-subagents Verify，。