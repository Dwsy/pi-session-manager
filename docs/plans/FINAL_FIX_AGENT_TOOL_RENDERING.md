# Agent Fix - > 「はSuccessの」—— FailedSuccess---## Issue**Error**：- ❌ `Message.details` Type `SubagentDetails`- ❌ @tintinweb `AgentDetails` Type- ❌ TypeScript TypeData**Data**：```@tintinweb Agent ↓{ details: AgentDetails } // ↓SaveSession JSONL ↓Pi Session Manager ↓SubagentToolCall Component ↓❌ Type，！```---## Fix### 1. Message Type**File**: `src/types.ts````typescriptexport interface Message { // ... // + diff details?: SubagentDetails | TintinwebAgentDetails | { diff?: string }}```### 2. Update SubagentToolCall Props**File**: `src/components/SubagentToolCall.tsx````typescriptinterface SubagentToolCallProps { arguments?: Record<string, any> details?: SubagentDetails | TintinwebAgentDetails // output?: string entryId?: string searchQuery?: string}```### 3. Update ToolCallList Type**File**: `src/components/ToolCallList.tsx````typescriptcase 'subagent': return ( <SubagentToolCall arguments={args} details={result?.message?.details as SubagentDetails | TintinwebAgentDetails | undefined} output={output} entryId={entryId} /> )```### 4. Update toolCallDisplay Type**File**: `src/utils/toolCallDisplay.ts````typescriptcase 'subagent': { appendUniqueHtml(segments, output ? escapeHtml(output) : '') // results const details = result?.message?.details as SubagentDetails | TintinwebAgentDetails | undefined if (details && 'results' in details && Array.isArray(details.results)) { details.results.forEach((subagentResult) => { // ... }) } return segments}```---## File```src/types.ts ✅ Message.details Typesrc/components/SubagentToolCall.tsx ✅ Props TypeUpdatesrc/components/ToolCallList.tsx ✅ Typesrc/utils/toolCallDisplay.ts ✅ Type```---## ### @tintinweb ```Agent ↓{ name: "subagent", arguments: { prompt: "...", subagent_type: "Explore" }, result: { message: { details: { displayName: "Explore", status: "completed", turnCount: 5, toolUses: 3, tokens: "12.4k token", durationMs: 4500, ... } } }} ↓ToolCallList.tsx ↓SubagentToolCall.tsx ↓isTintinwebDetails(details) → true ↓TintinwebResultCard Component ↓✅ Display：✓ Explore [Explore] ⟳5 3 tool uses 12.4k token 4.5s```### ```Agent ↓{ name: "subagent", result: { message: { details: { mode: "parallel", results: [...] } } }} ↓ToolCallList.tsx ↓SubagentToolCall.tsx ↓isTintinwebDetails(details) → false ↓ ↓✅ Display：✓ Subagent [Parallel × 3] + 3 ```---## TypeScript Verify```bashnpx tsc --noEmit✅ Error```---## Design### Type？** A**: `any`- ❌ Type- ❌ IDE ** B**: Interface- ❌ Data- ❌ ** C**: Type（）✅- ✅ Type- ✅ - ✅ Type### TypeImplementation```typescriptfunction isTintinwebDetails(details): details is TintinwebAgentDetails { if (!details) return false return 'status' in details && 'displayName' in details && !('mode' in details)}```****：- `status` `displayName` → @tintinweb - `mode` `results` → - ，---## TestVerify### Test1. ** @tintinweb/pi-subagents** ```bash pi install npm:@tintinweb/pi-subagents ```2. **** ``` Agent({ subagent_type: "Explore", prompt: "Find all files that handle authentication", description: "Find auth files" }) ```3. ** Pi Session Manager ** - OpenSessionList - Agent Session - Verify： ``` ✓ Explore [Explore] ⟳5 3 tool uses 12.4k token 4.5s ⎿ searching, reading 3 files… ```---## ### Fix（Error）- ❌ Type- ❌ Update Message.details Type- ❌ DataType### Fix（）- ✅ Fix（Message Type）- ✅ （ToolCallList → SubagentToolCall）- ✅ Type- ✅ TypeScript ### **TypeData！**```Backend → SessionEntry → Message → details → Component ↓ ↓ ↓ ↓ ↓TypeData！```---## State```┌─────────────────────────────────────────┐│ Agent Fix │├─────────────────────────────────────────┤│ State：✅ Complete ││ TypeScript: ✅ ││ Data：✅ Type ││ ：✅ │└─────────────────────────────────────────┘```---## | Component | @tintinweb | | State ||------|----------------|-----------|------|| **meta.json ** | ✅ | ✅ | ✅ Complete || **turns ** | ✅ | ✅ | ✅ Complete || **Agent ** | ✅ | ✅ | ✅ Complete || **** | N/A | ✅ | ✅ Complete || **Type** | ✅ | ✅ | ✅ Complete |---> 「びき」—— **Fix**: 3 ****: Type**TestState**: TypeScript ✅ Agent ！(◕ᴗ◕✿)