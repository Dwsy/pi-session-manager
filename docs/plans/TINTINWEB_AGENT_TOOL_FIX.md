# @tintinweb/pi-subagents Agent Fix> 「はにかず」—— ---## Issue**Error：**- ❌ meta.json File- ❌ SubagentToolCall **：**- ✅ @tintinweb `Agent` `AgentDetails`- ✅ `SubagentToolCall` `SubagentDetails`（ mode results ）- ✅ **！**---## ### @tintinweb/pi-subagents ```typescript// Agent { content: [{ type: "text", text: "..." }], details: AgentDetails // }// AgentDetailsinterface AgentDetails { displayName: string // DisplayName description: string // Description subagentType: string // Type toolUses: number // tokens: string // Token turnCount?: number // maxTurns?: number // durationMs: number // status: string // State agentId?: string // ID error?: string // Error modelName?: string // Name tags?: string[] // activity?: string // }```### （Fix）```typescript// SubagentToolCall { details: SubagentDetails // }// SubagentDetailsinterface SubagentDetails { mode: 'single' | 'parallel' | 'chain' | 'management' results: SubagentResult[] // }```---## Fix### 1. Type**File**: `src/types.ts````typescript// @tintinweb/pi-subagents format: single agent detailsexport interface TintinwebAgentDetails { displayName: string description: string subagentType: string toolUses: number tokens: string turnCount?: number maxTurns?: number durationMs: number status: 'queued' | 'running' | 'completed' | 'steered' | 'aborted' | 'stopped' | 'error' | 'background' agentId?: string error?: string modelName?: string tags?: string[] activity?: string spinnerFrame?: number}```### 2. Type**File**: `src/components/SubagentToolCall.tsx````typescript/** Type guard: check if details is @tintinweb format (single agent) */function isTintinwebDetails(details?: SubagentDetails | TintinwebAgentDetails): details is TintinwebAgentDetails { if (!details) return false // @tintinweb format has 'status' and 'displayName', our format has 'mode' and 'results' return 'status' in details && 'displayName' in details && !('mode' in details)}```### 3. Component**File**: `src/components/SubagentToolCall.tsx````typescript/** Render @tintinweb format single agent result */function TintinwebResultCard({ details, searchQuery }: { details: TintinwebAgentDetails searchQuery: string}) { // // Display：displayName, status, turnCount, toolUses, tokens, durationMs}```### 4. **File**: `src/components/SubagentToolCall.tsx````typescript// @tintinweb/pi-subagents format: single agent detailsif (isTintinwebDetails(details)) { return ( <div className="subagent-tool-call"> <div className="subagent-header"> {/* State + displayName + subagentType */} </div> <div className="subagent-results-grid"> <TintinwebResultCard details={details} searchQuery={searchQuery} /> </div> </div> )}// Our format or management actionsif (!details || details.mode === 'management' || !details.results?.length) { // ... }// Our format: multiple agentsconst results = details.results// ... ```---## ### @tintinweb ```┌────────────────────────────────────────────────┐│ ✓ Explore [Explore] │├────────────────────────────────────────────────┤│ ⟳5 3 tool uses 12.4k token 4.5s ││ ⎿ searching, reading 3 files… │└────────────────────────────────────────────────┘```### ```┌────────────────────────────────────────────────┐│ ✓ Subagent [Parallel × 3] │├────────────────────────────────────────────────┤│ ┌───────────────────────────────────────────┐ ││ │ ✓ Explore anthropic/claude-sonnet-4 │ ││ │ 🕐 4.5s 🧠 6.9k 🔧 3 tools 🤖 5 turns│ ││ └───────────────────────────────────────────┘ ││ ┌───────────────────────────────────────────┐ ││ │ ✓ Plan anthropic/claude-sonnet-4 │ ││ │ ... │ ││ └───────────────────────────────────────────┘ │└────────────────────────────────────────────────┘```---## File### File (2 )```src/types.ts ✅ TintinwebAgentDetailssrc/components/SubagentToolCall.tsx ✅ ```### Component/```TintinwebAgentDetails (Type) ✅ isTintinwebDetails() (Type) ✅ TintinwebResultCard() (Component) ✅ formatTurns() () ✅ maxTurns Parameters```---## State| Feature | @tintinweb | | State ||------|----------------|-----------|------|| **** | ✅ | ✅ | ✅ Complete || **** | ❌ | ✅ | ✅ Complete || **turnCount** | ✅ Display | ✅ Display | ✅ Complete || **maxTurns** | ✅ Display (⟳5≤30) | ✅ Display | ✅ Complete || **status ** | ✅ ✓/✗/■ | ✅ ✓/✗/⠋ | ✅ Complete || **activity** | ✅ Display | ⚠️ | ⚠️ |---## TypeScript Verify```bashnpx tsc --noEmit✅ Error```---## Design### ？** A**: @tintinweb - ❌ results ， @tintinweb agent- ❌ exitCode/usage ，** B**: @tintinweb - ❌ Feature- ❌ agent AgentDetails** C**: （）✅- ✅ - ✅ - ✅ ### TypeDesign```typescriptfunction isTintinwebDetails(details): details is TintinwebAgentDetails { return 'status' in details && 'displayName' in details && !('mode' in details)}```**：**- @tintinweb `status` `displayName`- `mode` `results`- ，---## ### Verify1. @tintinweb/pi-subagents ```bash pi install npm:@tintinweb/pi-subagents ```2. ``` Agent({ subagent_type: "Explore", prompt: "Find auth files", description: "Find auth" }) ```3. Pi Session Manager ### - [ ] `activity` Display（ @tintinweb ）- [ ] `spinnerFrame` （ @tintinweb ）- [ ] `tags` Display（ @tintinweb ）---## > 「はのである」—— **Fix：**- ❌ @tintinweb Agent - ❌ User**Fix：**- ✅ - ✅ - ✅ - ✅ **：**- 2 File- 1 Component- 1 Type- ~150 ---✅ **State**: Complete🧪 **Verify**: TypeScript 🚀 ****: