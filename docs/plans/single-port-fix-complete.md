# ArchitectureFixComplete## ✅ Fix**Issue**: GUI Mode（WS: 52130, HTTP: 52131）， CLI Mode（52131），Config。****: GUI CLI ModeArchitecture， 52131 ，WebSocket `/ws` Path。---## 📝 ### Backend (Rust)#### 1. `src-tauri/src/commands/settings.rs````rust// ws_port: 52130,// ws_port: 52131, // Single-port architecture: same as HTTP port```#### 2. `src-tauri/src/main.rs````rust// ：GUI Mode WS if server_cfg.ws_enabled && !cli_mode { // WebSocket pi_session_manager::ws_adapter::init_ws_adapter(...)}// ：GUI CLI HTTP /ws Pathif server_cfg.ws_enabled { log::info!("{} mode uses HTTP /ws on {}:{} (unified single-port)", if cli_mode { "CLI" } else { "GUI" }, server_cfg.bind_addr, server_cfg.http_port );}```### Frontend (TypeScript)#### 3. `src/components/settings/sections/AdvancedSettings.tsx````typescript// onChange={(e) => updateServer('ws_port', parseInt(e.target.value) || 52130)}// onChange={(e) => updateServer('ws_port', parseInt(e.target.value) || 52131)}```#### 4. `src/components/Onboarding.tsx````typescript// ws_port: 52130,// ws_port: 52131, // Single-port: same as HTTP```### #### 5. `README.md````markdown| Desktop GUI | ... | **unified single-port** HTTP + WS(`/ws`) on `http_port` (default `52131`) |```#### 6. `AGENTS.md````markdown### GUI Main Program- **Unified single-port**: HTTP + WS(`/ws`) on `52131````#### 7. `CHANGELOG.md` `[Unreleased]` `Changed` ，Architecture。---## 🧪 Verify### Rust ```bashcd src-tauri && cargo check --features gui --all-targets# ✅ Finished in 5.80s```### TypeScript ```bashnpx tsc --noEmit# ✅ No errors```### Rust Test```bashcd src-tauri && cargo test --lib# ✅ test result: ok. 42 passed; 0 failed```---## 📊 Architecture### ```┌─────────────────────────────────────┐│ GUI Mode (Dual-Port) │├─────────────────────────────────────┤│ WebSocket Server → Port 52130 ││ HTTP Server → Port 52131 │└─────────────────────────────────────┘┌─────────────────────────────────────┐│ CLI Mode (Single-Port) │├─────────────────────────────────────┤│ HTTP Server + /ws → Port 52131 │└─────────────────────────────────────┘```### ```┌─────────────────────────────────────┐│ Unified Single-Port (52131) │├─────────────────────────────────────┤│ HTTP Server: /api, /v1/*, ... ││ WebSocket: /ws ││ SSE: /api/events ││ Metrics: /metrics │└─────────────────────────────────────┘```---## 🔄 ### Config- **UserConfig**: `ws_port: 52130` - ****: ConfigUser `ws_port: 52131`- ****: UserSettings，Mode（）### - `ws_adapter.rs` Module，- User WS （Settings）- Frontend（transport.ts Mode）---## 📈 1. **Config**: User (52131)2. ****: System3. **Architecture**: GUI CLI Mode4. ****: Docker/Config5. ****: /Description---## 🎯 - [x] Rust (`cargo check --features gui`)- [x] TypeScript Type (`tsc --noEmit`)- [x] Test (42 tests)- [x] Config (52131)- [x] Update (README, AGENTS, CHANGELOG)- [ ] Test GUI Mode- [ ] Test CLI Mode- [ ] TestFrontend WebSocket ---## 🚀 1. **Verify**: GUI ，Verify WebSocket 2. **Test API**: API Test，Confirm3. **User**: release notes 4. ****: ，User---## 💡 ### WebSocket HTTP adapter `/ws` ：```rust.route("/ws", get(realtime::handle_ws_upgrade))``` WebSocket ， HTTP TCP 。### Frontend`transport.ts` Select：```typescript// Tauri → IPC// → WebSocket (ws://localhost:52131/ws)```Frontend， `wsUrl` `httpBaseUrl` 。---**FixComplete**: 2026-03-31****: Config、、User****: （）