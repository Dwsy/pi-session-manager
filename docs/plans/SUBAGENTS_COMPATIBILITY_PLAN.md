# @tintinweb/pi-subagents 兼容性计划> 「成長は昨日の自分を超え続けることだ」—— 成长就是不断超越昨天的自己---## 1. 现状分析### 1.1 项目现有子代理实现**架构概览：**```Pi Session Manager 子代理系统├── 前端 (React/TypeScript)│   ├── SubagentToolCall.tsx      # 工具调用渲染组件│   ├── SubagentModal.tsx         # 会话详情弹窗│   ├── types.ts                  # SubagentResult/SubagentDetails 类型│   └── styles/subagent.css       # 样式定义│├── 后端 (Rust/Tauri)│   ├── src/subagent.rs           # 元数据解析与聚合│   ├── src/sqlite_cache/subagent_meta.rs  # SQLite 缓存│   ├── src/models.rs             # SubagentRunInfo/AgentStats/SubagentSummary│   └── tests/subagent_cost_test.rs # 集成测试│└── 数据流    ├── meta.json 解析 → SubagentRunInfo    ├── 多会话扫描 → SubagentSummary    └── SQLite 缓存 (path + file_mtime)```**核心特性：**- ✅ 扫描 `subagent-artifacts/*_meta.json` 文件- ✅ 解析 usage (input/output/cacheRead/cacheWrite/cost/turns)- ✅ 按 agent 类型和 model 聚合统计- ✅ SQLite 缓存避免重复解析- ✅ 前端展示工具调用卡片 + 会话详情弹窗- ✅ 渐进式渲染大量会话条目**数据模型：**```rustpub struct SubagentRunInfo {    pub run_id: String,    pub agent: String,    pub model: String,    pub exit_code: i32,    pub cost: f64,    pub input_tokens: usize,    pub output_tokens: usize,    pub cache_read_tokens: usize,    pub cache_write_tokens: usize,    pub duration_ms: u64,    pub tool_count: usize,    pub timestamp: i64,}pub struct SubagentSummary {    pub total_cost: f64,    pub total_runs: usize,    pub total_tokens: usize,    pub runs_by_agent: HashMap<String, AgentStats>,    pub runs_by_model: HashMap<String, f64>,}```### 1.2 @tintinweb/pi-subagents npm 包特性**核心能力：**- 🎯 **Claude Code 风格**：`Agent` / `get_subagent_result` / `steer_subagent` 工具- 🔄 **并行后台代理**：可配置并发限制（默认 4），自动队列- 📊 **实时 Widget UI**：编辑器上方持久化组件，显示活动状态/Token/工具使用- 💬 **会话查看器**：实时滚动覆盖层，自动跟随新内容- 🛠️ **自定义代理类型**：`.pi/agents/<name>.md` + YAML frontmatter- 🎮 **中途转向**：向运行中的代理注入消息- 📝 **会话恢复**：保留完整上下文继续执行- 🕒 **优雅轮次限制**：达到 max_turns 后给予 5 个宽限轮次- 🔤 **大小写不敏感**：`"explore"` / `"Explore"` / `"EXPLORE"` 等效- 🎲 **模糊模型选择**：`"haiku"` / `"sonnet"` 自动匹配已配置模型- 🧠 **上下文继承**：可选 fork 父会话对话- 💾 **持久化记忆**：project/local/user 三种作用域- 🌳 **Git worktree 隔离**：独立 repo 副本，变更自动提交到分支- 📦 **技能预加载**：从 `.pi/skills/` 注入技能文件- 🚫 **工具黑名单**：`disallowed_tools` frontmatter- 🚌 **事件总线**：`subagents:*` 生命周期事件- 🔌 **跨扩展 RPC**：通过 `pi.events` 生成/停止子代理**默认代理类型：**| 类型 | 工具 | 模型 | Prompt 模式 | 描述 ||------|------|------|------------|------|| `general-purpose` | 全部 7 个 | 继承 | `append` (父代理 twin) | 继承父代理完整系统提示 || `Explore` | read/bash/grep/find/ls | haiku | `replace` (独立) | 快速代码探索（只读） || `Plan` | read/bash/grep/find/ls | 继承 | `replace` (独立) | 软件架构规划（只读） |**Frontmatter 字段：**```yamldescription: string              # 代理描述display_name: string             # UI 显示名称tools: string                    # 逗号分隔的内置工具extensions: boolean              # 继承 MCP/扩展工具skills: string|boolean           # 继承技能或指定技能列表memory: string                   # project/local/userdisallowed_tools: string         # 禁止的工具isolation: "worktree"            # Git worktree 隔离model: string                    # 模型（支持模糊匹配）thinking: string                 # 思考级别max_turns: number                # 最大轮次prompt_mode: "replace"|"append"  # Prompt 模式inherit_context: boolean         # 继承父上下文run_in_background: boolean       # 默认后台运行isolated: boolean                # 无扩展/MCP 工具enabled: boolean                 # 是否启用```**工具定义：**```typescript// Agent 工具Agent({  prompt: string,           // 必需  description: string,      // 必需  subagent_type: string,    // 必需  model?: string,  thinking?: string,  max_turns?: number,  run_in_background?: boolean,  resume?: string,          // 恢复会话 ID  isolated?: boolean,  isolation?: "worktree",  inherit_context?: boolean,})// get_subagent_result 工具get_subagent_result({  agent_id: string,  wait?: boolean,  verbose?: boolean,})// steer_subagent 工具steer_subagent({  agent_id: string,  message: string,})```**事件总线：**```typescriptsubagents:created     // 后台代理注册subagents:started     // 代理开始运行subagents:completed   // 代理成功完成subagents:failed      // 代理失败/停止/中止subagents:steered     // 发送转向消息subagents:ready       // 扩展加载完成```---## 2. 差距分析 (Gap Analysis)### 2.1 功能差距| 功能 | 当前实现 | @tintinweb/pi-subagents | 差距 | 优先级 ||------|---------|------------------------|------|--------|| **代理类型系统** | ❌ 无 | ✅ 3 个默认 + 自定义 .md | 高 | P0 || **Frontmatter 配置** | ❌ 无 | ✅ YAML frontmatter | 高 | P0 || **Agent 工具** | ❌ 无 | ✅ Agent 工具调用 | 高 | P0 || **get_subagent_result** | ❌ 无 | ✅ 查询后台代理 | 高 | P0 || **steer_subagent** | ❌ 无 | ✅ 中途转向 | 中 | P1 || **实时 Widget** | ❌ 无 | ✅ 编辑器上方组件 | 中 | P1 || **事件总线** | ❌ 无 | ✅ pi.events 生命周期事件 | 中 | P1 || **跨扩展 RPC** | ❌ 无 | ✅ subagents:rpc:* | 低 | P2 || **会话恢复** | ⚠️ 部分 (文件路径) | ✅ resume 参数 + 完整上下文 | 中 | P1 || **优雅轮次限制** | ❌ 无 | ✅ max_turns + 5 宽限轮 | 低 | P2 || **模糊模型匹配** | ❌ 无 | ✅ "haiku" 自动匹配 | 低 | P2 || **上下文继承** | ❌ 无 | ✅ inherit_context | 低 | P2 || **Git worktree 隔离** | ❌ 无 | ✅ isolation: worktree | 低 | P2 || **技能预加载** | ❌ 无 | ✅ skills frontmatter | 低 | P2 || **工具黑名单** | ❌ 无 | ✅ disallowed_tools | 低 | P2 || **并发队列** | ❌ 无 | ✅ 并发限制 + 自动队列 | 中 | P1 || **Join 策略** | ❌ 无 | ✅ smart/async/group | 低 | P2 |### 2.2 数据模型差距**当前 meta.json 格式 (@tintinweb)：**```json{  "runId": "abc123",  "agent": "worker",  "model": "claude-opus-4-6:minimal",  "exitCode": 0,  "usage": {    "input": 13,    "output": 1678,    "cacheRead": 127270,    "cacheWrite": 16527,    "cost": 0.208,    "turns": 11  },  "durationMs": 44428,  "toolCount": 13,  "timestamp": 1771087154366}```**项目当前 SubagentRunInfo：**```rustpub struct SubagentRunInfo {    pub run_id: String,    pub agent: String,    pub model: String,    pub exit_code: i32,    pub cost: f64,    pub input_tokens: usize,    pub output_tokens: usize,    pub cache_read_tokens: usize,    pub cache_write_tokens: usize,    pub duration_ms: u64,    pub tool_count: usize,    pub timestamp: i64,}```✅ **好消息**：字段基本对齐，只需微调命名映射### 2.3 架构差距**@tintinweb 架构：**```pi Extension (TypeScript)├── Agent Registry (.md 文件发现)├── Agent Spawner (Agent 工具实现)├── Queue Manager (并发控制)├── Event Bus (pi.events)├── Widget Renderer (UI)└── RPC Handler (跨扩展通信)```**项目当前架构：**```Tauri App (Rust + React)├── Session Scanner (文件扫描)├── Meta Parser (JSON 解析)├── SQLite Cache (缓存层)├── Stats Aggregator (聚合统计)└── UI Renderer (结果展示)```**关键差异：**- @tintinweb 是 **主动执行器**（生成/管理/运行子代理）- 项目当前是 **被动分析器**（扫描/解析/展示已有子代理记录）---## 3. 兼容性策略### 3.1 核心原则> 「単純さは究極の洗練である」—— 简单是终极的精致**策略选择：渐进式兼容（非完全重写）**1. **保持现有扫描功能**：继续支持扫描 `subagent-artifacts/*_meta.json`2. **新增 Agent 工具实现**：在 Rust 后端实现 `spawn_subagent` 命令3. **新增代理类型系统**：支持 `.pi/agents/*.md` 发现与解析4. **新增事件总线**：通过 Tauri events 发射生命周期事件5. **前端 UI 增强**：添加实时 Widget（可选）### 3.2 兼容层次```┌─────────────────────────────────────────┐│ L4: 运行时兼容 (执行子代理)              │ ← 远期目标├─────────────────────────────────────────┤│ L3: 工具兼容 (Agent/steer/get_result)   │ ← P1 阶段├─────────────────────────────────────────┤│ L2: 数据兼容 (meta.json 格式)           │ ← 已完成 90%├─────────────────────────────────────────┤│ L1: 类型兼容 (.md 代理定义)             │ ← P0 阶段└─────────────────────────────────────────┘```---## 4. 实施计划### Phase 0: 数据层对齐（P0 - 1 天）**目标**：确保 meta.json 格式完全兼容**任务：**- [ ] 验证当前 `parse_meta_json` 处理所有 @tintinweb 字段- [ ] 添加缺失字段：`turns`（已存在 usage.turns 但未映射）- [ ] 添加 `progressSummary` 字段支持（toolCount/tokens/durationMs）- [ ] 更新 TypeScript 类型定义对齐**代码改动：**```rust// src-tauri/src/subagent.rspub fn parse_meta_json(content: &str) -> Option<SubagentRunInfo> {    let v: serde_json::Value = serde_json::from_str(content).ok()?;    let usage = v.get("usage").cloned().unwrap_or(serde_json::Value::Null);    Some(SubagentRunInfo {        // ... 现有字段        turns: usage.get("turns").and_then(|x| x.as_u64()).unwrap_or(0) as usize, // 新增    })}```### Phase 1: 代理类型系统（P0 - 2-3 天）**目标**：支持 `.pi/agents/*.md` 发现与解析**任务：**- [ ] 创建 `AgentType` 结构体（frontmatter 解析）- [ ] 实现代理发现（`.pi/agents/` + `~/.pi/agent/agents/`）- [ ] 实现 YAML frontmatter 解析（使用 `serde_yaml`）- [ ] 实现默认代理类型（general-purpose/Explore/Plan）- [ ] 实现代理类型覆盖逻辑（项目 > 全局 > 默认）- [ ] 添加 Tauri 命令：`list_agent_types` / `get_agent_type` / `eject_agent_type`**数据结构：**```rust// src-tauri/src/agent_types.rs#[derive(Debug, Clone, Serialize, Deserialize)]pub struct AgentType {    pub name: String,    pub source: AgentSource, // Project | Global | Default    pub enabled: bool,    // Frontmatter 字段    pub description: Option<String>,    pub display_name: Option<String>,    pub tools: Option<Vec<Tool>>,    pub extensions: Option<bool>,    pub skills: Option<SkillsConfig>,    pub memory: Option<MemoryScope>,    pub disallowed_tools: Option<Vec<String>>,    pub model: Option<String>,    pub thinking: Option<ThinkingLevel>,    pub max_turns: Option<u32>,    pub prompt_mode: Option<PromptMode>,    pub inherit_context: Option<bool>,    pub run_in_background: Option<bool>,    pub isolated: Option<bool>,    pub isolation: Option<IsolationMode>,    // Prompt 正文    pub prompt_body: String,}#[derive(Debug, Clone, Serialize, Deserialize)]pub enum AgentSource {    Default,    Global(PathBuf),    Project(PathBuf),}```### Phase 2: Agent 工具实现（P1 - 3-4 天）**目标**：实现 `Agent` 工具调用**任务：**- [ ] 设计 `spawn_subagent` 命令接口- [ ] 实现子代理生成逻辑（调用 pi CLI 或其他 agent）- [ ] 实现并发队列管理（默认 4 个并发）- [ ] 实现后台代理状态跟踪- [ ] 实现 `get_subagent_result` 命令- [ ] 实现 `steer_subagent` 命令- [ ] 添加 SQLite 表：`active_subagents` (状态跟踪)**命令接口：**```rust#[derive(Debug, Serialize, Deserialize)]pub struct SpawnSubagentArgs {    pub prompt: String,    pub description: String,    pub subagent_type: String,    pub model: Option<String>,    pub thinking: Option<String>,    pub max_turns: Option<u32>,    pub run_in_background: Option<bool>,    pub resume: Option<String>,    pub isolated: Option<bool>,    pub isolation: Option<String>,    pub inherit_context: Option<bool>,}#[tauri::command]pub async fn spawn_subagent(    args: SpawnSubagentArgs,    state: State<'_, SubagentManager>,) -> Result<SpawnSubagentResult, String> {    // 1. 查找代理类型    // 2. 解析 frontmatter    // 3. 构建子代理命令    // 4. 启动子代理（后台/前台）    // 5. 返回 agent_id}```### Phase 3: 事件总线（P1 - 1-2 天）**目标**：实现生命周期事件发射**任务：**- [ ] 定义事件类型枚举- [ ] 实现 Tauri event emitter- [ ] 在关键节点发射事件：  - `subagents:created`  - `subagents:started`  - `subagents:completed`  - `subagents:failed`  - `subagents:steered`- [ ] 前端监听事件并更新 UI**事件结构：**```rust// src-tauri/src/events.rs#[derive(Debug, Clone, Serialize)]pub struct SubagentEvent {    pub event_type: SubagentEventType,    pub agent_id: String,    pub timestamp: i64,    pub data: serde_json::Value,}#[derive(Debug, Clone, Serialize)]#[serde(tag = "type")]pub enum SubagentEventType {    Created { subagent_type: String, description: String },    Started { subagent_type: String },    Completed { duration_ms: u64, tokens: u64, tool_uses: usize },    Failed { error: String, status: String },    Steered { message: String },}```### Phase 4: 前端 UI 增强（P1 - 2-3 天）**目标**：添加实时 Widget 和代理管理界面**任务：**- [ ] 创建 `AgentWidget` 组件（编辑器上方）- [ ] 实现代理状态显示（运行中/排队/完成）- [ ] 添加 `/agents` 命令面板- [ ] 实现代理类型管理（查看/启用/禁用/导出）- [ ] 实现代理会话查看器（实时滚动）- [ ] 添加转向消息输入框**组件结构：**```src/components/agents/├── AgentWidget.tsx          # 实时状态组件├── AgentManagement.tsx      # /agents 命令面板├── AgentTypeList.tsx        # 代理类型列表├── AgentSessionViewer.tsx   # 会话查看器└── SteerMessageInput.tsx    # 转向消息输入```### Phase 5: 高级特性（P2 - 按需）**目标**：实现增强功能**任务：**- [ ] Git worktree 隔离- [ ] 技能预加载- [ ] 模糊模型匹配- [ ] 优雅轮次限制- [ ] Join 策略（smart/async/group）- [ ] 跨扩展 RPC---## 5. 技术决策### 5.1 子代理执行策略**选项 A：调用 pi CLI**```bashpi -p "任务描述" --session <session-id>```- ✅ 简单，复用现有 pi 能力- ❌ 依赖外部 pi 安装- ❌ 难以精细控制工具/模型**选项 B：直接调用模型 API**```rust// 内部实现 agent looplet mut turns = 0;while turns < max_turns {    let response = call_model(&prompt, &tools).await?;    // 处理工具调用...}```- ✅ 完全控制- ❌ 实现复杂度高- ❌ 需要实现工具系统**选项 C：混合策略（推荐）**- 使用 `interactive_shell` 技能启动 pi 子代理- 通过文件/事件总线通信- 支持 foreground/background 模式**决策**：Phase 1-2 采用 **选项 C**，长期可演进到选项 B### 5.2 并发控制**设计：**```rustpub struct SubagentQueue {    max_concurrency: usize,    running: DashMap<String, AgentHandle>,    queued: VecDeque<SpawnRequest>,}impl SubagentQueue {    pub async fn spawn(&self, request: SpawnRequest) -> Result<String> {        if self.running.len() >= self.max_concurrency {            // 加入队列            self.queued.push_back(request);            return Ok(queue_id);        }        // 立即启动        let handle = self.launch(request).await?;        self.running.insert(agent_id, handle);        Ok(agent_id)    }}```### 5.3 状态持久化**SQLite 表设计：**```sqlCREATE TABLE active_subagents (    agent_id TEXT PRIMARY KEY,    subagent_type TEXT NOT NULL,    description TEXT NOT NULL,    status TEXT NOT NULL, -- pending/running/completed/failed/stopped    created_at INTEGER NOT NULL,    started_at INTEGER,    completed_at INTEGER,    prompt TEXT,    model TEXT,    thinking TEXT,    max_turns INTEGER,    current_turns INTEGER DEFAULT 0,    tool_count INTEGER DEFAULT 0,    tokens_input INTEGER DEFAULT 0,    tokens_output INTEGER DEFAULT 0,    cost REAL DEFAULT 0,    error_message TEXT,    session_path TEXT,    artifact_dir TEXT);CREATE INDEX idx_subagents_status ON active_subagents(status);CREATE INDEX idx_subagents_created ON active_subagents(created_at);```---## 6. 验收标准### Phase 0 验收- [ ] 能解析 @tintinweb 生成的所有 meta.json 变体- [ ] 单元测试覆盖率达到 90%- [ ] 现有测试全部通过### Phase 1 验收- [ ] 能发现 `.pi/agents/*.md` 文件- [ ] 能解析 YAML frontmatter 所有字段- [ ] 默认代理类型可用（general-purpose/Explore/Plan）- [ ] 代理类型覆盖逻辑正确- [ ] `list_agent_types` 命令返回正确结果### Phase 2 验收- [ ] 能成功生成子代理（Explore 类型）- [ ] 后台代理状态可查询- [ ] 能获取后台代理结果- [ ] 能发送转向消息- [ ] 并发队列正常工作（限制 4 个并发）### Phase 3 验收- [ ] 所有生命周期事件正确发射- [ ] 前端能监听并响应事件- [ ] 事件数据完整（包含所有必需字段）### Phase 4 验收- [ ] Agent Widget 正确显示运行中代理- [ ] `/agents` 命令面板功能完整- [ ] 代理类型管理界面可用- [ ] 会话查看器实时滚动正常---## 7. 风险与缓解| 风险 | 影响 | 概率 | 缓解措施 ||------|------|------|----------|| **pi CLI 接口变化** | 高 | 中 | 抽象适配层，隔离变化 || **并发队列死锁** | 高 | 低 | 添加超时机制，单元测试覆盖 || **事件总线性能** | 中 | 中 | 限制事件频率，批量发射 || **前端 Widget 卡顿** | 中 | 中 | 虚拟滚动，限制更新频率 || **meta.json 格式不兼容** | 高 | 低 | 宽容解析，默认值兜底 |---## 8. 时间估算| Phase | 复杂度 | 估算时间 | 依赖 ||-------|--------|----------|------|| Phase 0 | 低 | 1 天 | 无 || Phase 1 | 中 | 2-3 天 | Phase 0 || Phase 2 | 高 | 3-4 天 | Phase 1 || Phase 3 | 低 | 1-2 天 | Phase 2 || Phase 4 | 中 | 2-3 天 | Phase 3 || Phase 5 | 低 | 按需 | Phase 4 |**总计**：9-13 天（不含 Phase 5）---## 9. 下一步行动### 立即执行（本周）1. **Phase 0**：数据层对齐   - 添加 `turns` 字段映射   - 更新 TypeScript 类型   - 添加单元测试2. **Phase 1 启动**：代理类型系统   - 创建 `AgentType` 结构体   - 实现 YAML frontmatter 解析   - 实现默认代理发现### 下周3. **Phase 1 完成**：代理类型管理命令4. **Phase 2 启动**：Agent 工具实现---## 附录 A：相关文件清单### 需要创建的文件```src-tauri/src/agent_types.rs           # 代理类型定义与发现src-tauri/src/subagent_manager.rs      # 子代理管理器src-tauri/src/subagent_queue.rs        # 并发队列src-tauri/src/events.rs                # 事件总线src-tauri/src/commands/subagents.rs    # Tauri 命令层src/components/agents/AgentWidget.tsx  # 实时 Widgetsrc/components/agents/AgentManagement.tsxsrc/components/agents/AgentTypeList.tsxsrc/components/agents/AgentSessionViewer.tsxdocs/adr/SUBAGENT_ARCHITECTURE.md      # 架构决策记录```### 需要修改的文件```src-tauri/src/models.rs                # 添加 AgentType 等类型src-tauri/src/lib.rs                   # 注册新命令src-tauri/src/sqlite_cache.rs          # 添加 active_subagents 表src/types.ts                           # 添加子代理相关类型src-tauri/Cargo.toml                   # 添加 serde_yaml 依赖```---> 「千里の道も一歩から」—— 千里之行，始于足下让我们从 Phase 0 开始，逐步实现完整的兼容性。