# @tintinweb/pi-subagents > 「はのをえけることだ」—— ---## 1. Analysis### 1.1 ProjectImplementation**Architecture：**```Pi Session Manager System├── Frontend (React/TypeScript)│ ├── SubagentToolCall.tsx # Component│ ├── SubagentModal.tsx # Session│ ├── types.ts # SubagentResult/SubagentDetails Type│ └── styles/subagent.css # │├── Backend (Rust/Tauri)│ ├── src/subagent.rs # Data│ ├── src/sqlite_cache/subagent_meta.rs # SQLite │ ├── src/models.rs # SubagentRunInfo/AgentStats/SubagentSummary│ └── tests/subagent_cost_test.rs # Test│└── Data ├── meta.json → SubagentRunInfo ├── Session → SubagentSummary └── SQLite (path + file_mtime)```**：**- ✅ `subagent-artifacts/*_meta.json` File- ✅ usage (input/output/cacheRead/cacheWrite/cost/turns)- ✅ agent Type model - ✅ SQLite - ✅ Frontend + Session- ✅ Session**Data：**```rustpub struct SubagentRunInfo { pub run_id: String, pub agent: String, pub model: String, pub exit_code: i32, pub cost: f64, pub input_tokens: usize, pub output_tokens: usize, pub cache_read_tokens: usize, pub cache_write_tokens: usize, pub duration_ms: u64, pub tool_count: usize, pub timestamp: i64,}pub struct SubagentSummary { pub total_cost: f64, pub total_runs: usize, pub total_tokens: usize, pub runs_by_agent: HashMap<String, AgentStats>, pub runs_by_model: HashMap<String, f64>,}```### 1.2 @tintinweb/pi-subagents npm **：**- 🎯 **Claude Code **：`Agent` / `get_subagent_result` / `steer_subagent` - 🔄 ****：Config（ 4），- 📊 ** Widget UI**：Component，DisplayState/Token/- 💬 **Session**：，Content- 🛠️ **Type**：`.pi/agents/<name>.md` + YAML frontmatter- 🎮 ****：Message- 📝 **SessionRestore**：- 🕒 ****： max_turns 5 - 🔤 ****：`"explore"` / `"Explore"` / `"EXPLORE"` - 🎲 **Select**：`"haiku"` / `"sonnet"` Config- 🧠 ****： fork Session- 💾 ****：project/local/user - 🌳 **Git worktree **： repo ，- 📦 **Load**： `.pi/skills/` File- 🚫 ****：`disallowed_tools` frontmatter- 🚌 ****：`subagents:*` - 🔌 ** RPC**： `pi.events` /**Type：**| Type | | | Prompt Mode | Description ||------|------|------|------------|------|| `general-purpose` | 7 | | `append` ( twin) | System || `Explore` | read/bash/grep/find/ls | haiku | `replace` () | （） || `Plan` | read/bash/grep/find/ls | | `replace` () | Architecture（） |**Frontmatter ：**```yamldescription: string # Descriptiondisplay_name: string # UI DisplayNametools: string # extensions: boolean # MCP/skills: string|boolean # Listmemory: string # project/local/userdisallowed_tools: string # isolation: "worktree" # Git worktree model: string # （）thinking: string # max_turns: number # prompt_mode: "replace"|"append" # Prompt Modeinherit_context: boolean # run_in_background: boolean # isolated: boolean # /MCP enabled: boolean # ```**：**```typescript// Agent Agent({ prompt: string, // description: string, // subagent_type: string, // model?: string, thinking?: string, max_turns?: number, run_in_background?: boolean, resume?: string, // RestoreSession ID isolated?: boolean, isolation?: "worktree", inherit_context?: boolean,})// get_subagent_result get_subagent_result({ agent_id: string, wait?: boolean, verbose?: boolean,})// steer_subagent steer_subagent({ agent_id: string, message: string,})```**：**```typescriptsubagents:created // subagents:started // subagents:completed // SuccessCompletesubagents:failed // Failed//subagents:steered // Messagesubagents:ready // LoadComplete```---## 2. Analysis (Gap Analysis)### 2.1 Feature| Feature | Implementation | @tintinweb/pi-subagents | | ||------|---------|------------------------|------|--------|| **TypeSystem** | ❌ | ✅ 3 + .md | | P0 || **Frontmatter Config** | ❌ | ✅ YAML frontmatter | | P0 || **Agent ** | ❌ | ✅ Agent | | P0 || **get_subagent_result** | ❌ | ✅ | | P0 || **steer_subagent** | ❌ | ✅ | | P1 || ** Widget** | ❌ | ✅ Component | | P1 || **** | ❌ | ✅ pi.events | | P1 || ** RPC** | ❌ | ✅ subagents:rpc:* | | P2 || **SessionRestore** | ⚠️ (FilePath) | ✅ resume Parameters + | | P1 || **** | ❌ | ✅ max_turns + 5 | | P2 || **** | ❌ | ✅ "haiku" | | P2 || **** | ❌ | ✅ inherit_context | | P2 || **Git worktree ** | ❌ | ✅ isolation: worktree | | P2 || **Load** | ❌ | ✅ skills frontmatter | | P2 || **** | ❌ | ✅ disallowed_tools | | P2 || **** | ❌ | ✅ + | | P1 || **Join ** | ❌ | ✅ smart/async/group | | P2 |### 2.2 Data** meta.json (@tintinweb)：**```json{ "runId": "abc123", "agent": "worker", "model": "claude-opus-4-6:minimal", "exitCode": 0, "usage": { "input": 13, "output": 1678, "cacheRead": 127270, "cacheWrite": 16527, "cost": 0.208, "turns": 11 }, "durationMs": 44428, "toolCount": 13, "timestamp": 1771087154366}```**Project SubagentRunInfo：**```rustpub struct SubagentRunInfo { pub run_id: String, pub agent: String, pub model: String, pub exit_code: i32, pub cost: f64, pub input_tokens: usize, pub output_tokens: usize, pub cache_read_tokens: usize, pub cache_write_tokens: usize, pub duration_ms: u64, pub tool_count: usize, pub timestamp: i64,}```✅ **Message**：，### 2.3 Architecture**@tintinweb Architecture：**```pi Extension (TypeScript)├── Agent Registry (.md File)├── Agent Spawner (Agent Implementation)├── Queue Manager ()├── Event Bus (pi.events)├── Widget Renderer (UI)└── RPC Handler ()```**ProjectArchitecture：**```Tauri App (Rust + React)├── Session Scanner (File)├── Meta Parser (JSON )├── SQLite Cache ()├── Stats Aggregator ()└── UI Renderer ()```**：**- @tintinweb ****（//）- Project **Analysis**（//）---## 3. ### 3.1 > 「さはのである」—— **Select：（）**1. **Feature**： `subagent-artifacts/*_meta.json`2. ** Agent Implementation**： Rust BackendImplementation `spawn_subagent` Command3. **TypeSystem**： `.pi/agents/*.md` 4. ****： Tauri events 5. **Frontend UI **： Widget（）### 3.2 ```┌─────────────────────────────────────────┐│ L4: () │ ← ├─────────────────────────────────────────┤│ L3: (Agent/steer/get_result) │ ← P1 ├─────────────────────────────────────────┤│ L2: Data (meta.json ) │ ← Complete 90%├─────────────────────────────────────────┤│ L1: Type (.md ) │ ← P0 └─────────────────────────────────────────┘```---## 4. ### Phase 0: Data（P0 - 1 ）****： meta.json **：**- [ ] Verify `parse_meta_json` @tintinweb - [ ] ：`turns`（ usage.turns ）- [ ] `progressSummary` （toolCount/tokens/durationMs）- [ ] Update TypeScript Type**：**```rust// src-tauri/src/subagent.rspub fn parse_meta_json(content: &str) -> Option<SubagentRunInfo> { let v: serde_json::Value = serde_json::from_str(content).ok()?; let usage = v.get("usage").cloned().unwrap_or(serde_json::Value::Null); Some(SubagentRunInfo { // ... turns: usage.get("turns").and_then(|x| x.as_u64()).unwrap_or(0) as usize, // })}```### Phase 1: TypeSystem（P0 - 2-3 ）****： `.pi/agents/*.md` **：**- [ ] `AgentType` （frontmatter ）- [ ] Implementation（`.pi/agents/` + `~/.pi/agent/agents/`）- [ ] Implementation YAML frontmatter （ `serde_yaml`）- [ ] ImplementationType（general-purpose/Explore/Plan）- [ ] ImplementationType（Project > > ）- [ ] Tauri Command：`list_agent_types` / `get_agent_type` / `eject_agent_type`**Data：**```rust// src-tauri/src/agent_types.rs#[derive(Debug, Clone, Serialize, Deserialize)]pub struct AgentType { pub name: String, pub source: AgentSource, // Project | Global | Default pub enabled: bool, // Frontmatter pub description: Option<String>, pub display_name: Option<String>, pub tools: Option<Vec<Tool>>, pub extensions: Option<bool>, pub skills: Option<SkillsConfig>, pub memory: Option<MemoryScope>, pub disallowed_tools: Option<Vec<String>>, pub model: Option<String>, pub thinking: Option<ThinkingLevel>, pub max_turns: Option<u32>, pub prompt_mode: Option<PromptMode>, pub inherit_context: Option<bool>, pub run_in_background: Option<bool>, pub isolated: Option<bool>, pub isolation: Option<IsolationMode>, // Prompt pub prompt_body: String,}#[derive(Debug, Clone, Serialize, Deserialize)]pub enum AgentSource { Default, Global(PathBuf), Project(PathBuf),}```### Phase 2: Agent Implementation（P1 - 3-4 ）****：Implementation `Agent` **：**- [ ] Design `spawn_subagent` CommandInterface- [ ] Implementation（ pi CLI agent）- [ ] Implementation（ 4 ）- [ ] ImplementationState- [ ] Implementation `get_subagent_result` Command- [ ] Implementation `steer_subagent` Command- [ ] SQLite ：`active_subagents` (State)**CommandInterface：**```rust#[derive(Debug, Serialize, Deserialize)]pub struct SpawnSubagentArgs { pub prompt: String, pub description: String, pub subagent_type: String, pub model: Option<String>, pub thinking: Option<String>, pub max_turns: Option<u32>, pub run_in_background: Option<bool>, pub resume: Option<String>, pub isolated: Option<bool>, pub isolation: Option<String>, pub inherit_context: Option<bool>,}#[tauri::command]pub async fn spawn_subagent( args: SpawnSubagentArgs, state: State<'_, SubagentManager>,) -> Result<SpawnSubagentResult, String> { // 1. Type // 2. frontmatter // 3. Command // 4. （/） // 5. agent_id}```### Phase 3: （P1 - 1-2 ）****：Implementation**：**- [ ] Type- [ ] Implementation Tauri event emitter- [ ] ： - `subagents:created` - `subagents:started` - `subagents:completed` - `subagents:failed` - `subagents:steered`- [ ] FrontendUpdate UI**：**```rust// src-tauri/src/events.rs#[derive(Debug, Clone, Serialize)]pub struct SubagentEvent { pub event_type: SubagentEventType, pub agent_id: String, pub timestamp: i64, pub data: serde_json::Value,}#[derive(Debug, Clone, Serialize)]#[serde(tag = "type")]pub enum SubagentEventType { Created { subagent_type: String, description: String }, Started { subagent_type: String }, Completed { duration_ms: u64, tokens: u64, tool_uses: usize }, Failed { error: String, status: String }, Steered { message: String },}```### Phase 4: Frontend UI （P1 - 2-3 ）****： Widget **：**- [ ] `AgentWidget` Component（）- [ ] ImplementationStateDisplay（//Complete）- [ ] `/agents` CommandPanel- [ ] ImplementationType（///Export）- [ ] ImplementationSession（）- [ ] Message**Component：**```src/components/agents/├── AgentWidget.tsx # StateComponent├── AgentManagement.tsx # /agents CommandPanel├── AgentTypeList.tsx # TypeList├── AgentSessionViewer.tsx # Session└── SteerMessageInput.tsx # Message```### Phase 5: （P2 - ）****：ImplementationFeature**：**- [ ] Git worktree - [ ] Load- [ ] - [ ] - [ ] Join （smart/async/group）- [ ] RPC---## 5. ### 5.1 ** A： pi CLI**```bashpi -p "Description" --session <session-id>```- ✅ ， pi - ❌ pi - ❌ /** B： API**```rust// Implementation agent looplet mut turns = 0;while turns < max_turns { let response = call_model(&prompt, &tools).await?; // ...}```- ✅ - ❌ Implementation- ❌ ImplementationSystem** C：（）**- `interactive_shell` pi - File/- foreground/background Mode****：Phase 1-2 ** C**， B### 5.2 **Design：**```rustpub struct SubagentQueue { max_concurrency: usize, running: DashMap<String, AgentHandle>, queued: VecDeque<SpawnRequest>,}impl SubagentQueue { pub async fn spawn(&self, request: SpawnRequest) -> Result<String> { if self.running.len() >= self.max_concurrency { // self.queued.push_back(request); return Ok(queue_id); } // let handle = self.launch(request).await?; self.running.insert(agent_id, handle); Ok(agent_id) }}```### 5.3 State**SQLite Design：**```sqlCREATE TABLE active_subagents ( agent_id TEXT PRIMARY KEY, subagent_type TEXT NOT NULL, description TEXT NOT NULL, status TEXT NOT NULL, -- pending/running/completed/failed/stopped created_at INTEGER NOT NULL, started_at INTEGER, completed_at INTEGER, prompt TEXT, model TEXT, thinking TEXT, max_turns INTEGER, current_turns INTEGER DEFAULT 0, tool_count INTEGER DEFAULT 0, tokens_input INTEGER DEFAULT 0, tokens_output INTEGER DEFAULT 0, cost REAL DEFAULT 0, error_message TEXT, session_path TEXT, artifact_dir TEXT);CREATE INDEX idx_subagents_status ON active_subagents(status);CREATE INDEX idx_subagents_created ON active_subagents(created_at);```---## 6. ### Phase 0 - [ ] @tintinweb meta.json - [ ] Test 90%- [ ] Test### Phase 1 - [ ] `.pi/agents/*.md` File- [ ] YAML frontmatter - [ ] Type（general-purpose/Explore/Plan）- [ ] Type- [ ] `list_agent_types` Command### Phase 2 - [ ] Success（Explore Type）- [ ] State- [ ] - [ ] Message- [ ] （ 4 ）### Phase 3 - [ ] - [ ] Frontend- [ ] Data（）### Phase 4 - [ ] Agent Widget Display- [ ] `/agents` CommandPanelFeature- [ ] Type- [ ] Session---## 7. | | | | ||------|------|------|----------|| **pi CLI Interface** | | | ， || **** | | | ，Test || **Performance** | | | ， || **Frontend Widget ** | | | ，Update || **meta.json ** | | | ， |---## 8. | Phase | | | ||-------|--------|----------|------|| Phase 0 | | 1 | || Phase 1 | | 2-3 | Phase 0 || Phase 2 | | 3-4 | Phase 1 || Phase 3 | | 1-2 | Phase 2 || Phase 4 | | 2-3 | Phase 3 || Phase 5 | | | Phase 4 |****：9-13 （ Phase 5）---## 9. ### （）1. **Phase 0**：Data - `turns` - Update TypeScript Type - Test2. **Phase 1 **：TypeSystem - `AgentType` - Implementation YAML frontmatter - Implementation### 3. **Phase 1 Complete**：TypeCommand4. **Phase 2 **：Agent Implementation---## A：File### File```src-tauri/src/agent_types.rs # Typesrc-tauri/src/subagent_manager.rs # src-tauri/src/subagent_queue.rs # src-tauri/src/events.rs # src-tauri/src/commands/subagents.rs # Tauri Commandsrc/components/agents/AgentWidget.tsx # Widgetsrc/components/agents/AgentManagement.tsxsrc/components/agents/AgentTypeList.tsxsrc/components/agents/AgentSessionViewer.tsxdocs/adr/SUBAGENT_ARCHITECTURE.md # Architecture```### File```src-tauri/src/models.rs # AgentType Typesrc-tauri/src/lib.rs # Commandsrc-tauri/src/sqlite_cache.rs # active_subagents src/types.ts # Typesrc-tauri/Cargo.toml # serde_yaml ```---> 「のもから」—— ， Phase 0 ，Implementation。