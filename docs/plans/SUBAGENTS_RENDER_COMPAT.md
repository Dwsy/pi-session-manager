# @tintinweb/pi-subagents 渲染兼容计划> 「简单は究極の洗練である」—— 简单是终极的精致## 核心目标**仅渲染兼容**：确保项目能正确解析和展示 `@tintinweb/pi-subagents` npm 包生成的子代理记录。**不实现**：子代理执行、Agent 工具、事件总线等运行时功能。---## 1. 数据格式分析### 1.1 @tintinweb 输出文件结构```/tmp/pi-subagents-{uid}/{encoded-cwd}/{sessionId}/tasks/├── {agentId}.output      # JSONL 会话记录└── {agentId}.meta.json   # 运行统计信息```### 1.2 .output 格式 (JSONL)```json{"isSidechain":true,"agentId":"abc123","type":"user","message":{"role":"user","content":"Find auth files"},"timestamp":"2026-03-26T10:00:00.000Z","cwd":"/project"}{"isSidechain":true,"agentId":"abc123","type":"assistant","message":{"role":"assistant","content":"I'll search...","toolCalls":[...]},"timestamp":"2026-03-26T10:00:05.000Z","cwd":"/project"}{"isSidechain":true,"agentId":"abc123","type":"toolResult","message":{"role":"toolResult","toolCallId":"...","content":"..."},"timestamp":"2026-03-26T10:00:10.000Z","cwd":"/project"}```**字段映射：**| @tintinweb 字段 | 项目字段 | 兼容性 ||----------------|---------|--------|| `type` | `entry.type` | ✅ 直接映射 || `message` | `entry.message` | ✅ 结构相同 || `timestamp` | `entry.timestamp` | ✅ ISO8601 || `agentId` | - | ⚠️ 项目当前无此字段 || `isSidechain` | - | ⚠️ 项目当前无此字段 || `cwd` | - | ⚠️ 项目当前无此字段 |### 1.3 .meta.json 格式```json{  "runId": "abc123",  "agent": "Explore",  "model": "anthropic/claude-sonnet-4",  "exitCode": 0,  "usage": {    "input": 1234,    "output": 5678,    "cacheRead": 0,    "cacheWrite": 0,    "cost": 0.015,    "turns": 5  },  "durationMs": 4500,  "toolCount": 3,  "timestamp": 1711450800000}```**字段对比：**| @tintinweb 字段 | 项目 SubagentRunInfo | 兼容性 ||----------------|---------------------|--------|| `runId` | `run_id` | ✅ 名称映射 || `agent` | `agent` | ✅ 完全一致 || `model` | `model` | ✅ 完全一致 || `exitCode` | `exit_code` | ✅ 名称映射 || `usage.input` | `input_tokens` | ✅ 映射 || `usage.output` | `output_tokens` | ✅ 映射 || `usage.cacheRead` | `cache_read_tokens` | ✅ 映射 || `usage.cacheWrite` | `cache_write_tokens` | ✅ 映射 || `usage.cost` | `cost` | ✅ 完全一致 || `usage.turns` | ❌ 缺失 | ⚠️ **需要添加** || `durationMs` | `duration_ms` | ✅ 名称映射 || `toolCount` | `tool_count` | ✅ 名称映射 || `timestamp` | `timestamp` | ✅ 完全一致 |---## 2. 差距分析### 2.1 Rust 后端差距**文件：`src-tauri/src/subagent.rs`**```rust// 当前代码 - 已正确处理大部分字段pub fn parse_meta_json(content: &str) -> Option<SubagentRunInfo> {    let v: serde_json::Value = serde_json::from_str(content).ok()?;    let usage = v.get("usage").cloned().unwrap_or(serde_json::Value::Null);    Some(SubagentRunInfo {        run_id: v.get("runId").and_then(|x| x.as_str()).unwrap_or("unknown").to_string(),        agent: v.get("agent").and_then(|x| x.as_str()).unwrap_or("unknown").to_string(),        model: v.get("model").and_then(|x| x.as_str()).unwrap_or("unknown").to_string(),        exit_code: v.get("exitCode").and_then(|x| x.as_i64()).unwrap_or(0) as i32,        cost: usage.get("cost").and_then(|x| x.as_f64()).unwrap_or(0.0),        input_tokens: usage.get("input").and_then(|x| x.as_u64()).unwrap_or(0) as usize,        output_tokens: usage.get("output").and_then(|x| x.as_u64()).unwrap_or(0) as usize,        cache_read_tokens: usage.get("cacheRead").and_then(|x| x.as_u64()).unwrap_or(0) as usize,        cache_write_tokens: usage.get("cacheWrite").and_then(|x| x.as_u64()).unwrap_or(0) as usize,        duration_ms: v.get("durationMs").and_then(|x| x.as_u64()).unwrap_or(0),        tool_count: v.get("toolCount").and_then(|x| x.as_u64()).unwrap_or(0) as usize,        timestamp: v.get("timestamp").and_then(|x| x.as_i64()).unwrap_or(0),        // ❌ 缺失：turns 字段    })}```**需要修改：**1. `src-tauri/src/models.rs` - 添加 `turns: usize` 字段到 `SubagentRunInfo`2. `src-tauri/src/subagent.rs` - 解析 `usage.turns`3. `src-tauri/src/sqlite_cache/subagent_meta.rs` - 更新缓存表结构### 2.2 TypeScript 前端差距**文件：`src/types.ts`**```typescript// 当前定义 - 基本兼容export interface SubagentResult {  agent: string  task: string  exitCode: number  model?: string  error?: string  usage?: {    input: number    output: number    cacheRead: number    cacheWrite: number    cost: number    turns: number  // ✅ 已存在  }  progressSummary?: { toolCount: number; tokens: number; durationMs: number }  artifactPaths?: SubagentArtifactPaths  sessionFile?: string  messages?: any[]  progress?: any}```**需要修改：**1. 添加 `outputFile?: string` 字段（@tintinweb 的 transcript 路径）2. 添加 `agentId?: string` 字段3. 考虑添加 `isSidechain?: boolean` 标识### 2.3 组件渲染差距**文件：`src/components/SubagentToolCall.tsx`**当前组件已支持：- ✅ 显示 agent 名称- ✅ 显示 model- ✅ 显示 exitCode 状态- ✅ 显示 usage 统计- ✅ 显示 progressSummary- ✅ 点击查看详情弹窗**可能需要调整：**1. 支持显示 `outputFile` 路径（如果有）2. 支持显示 `turns` 信息3. 确保 `messages` 数组格式兼容 @tintinweb 的输出**文件：`src/components/SubagentModal.tsx`**当前弹窗已支持：- ✅ 加载 JSONL 文件- ✅ 回退到 `result.messages[]`- ✅ 渲染 user/assistant/toolResult 消息- ✅ 渐进式渲染**潜在问题：**@tintinweb 的 JSONL 格式中 `entry.message` 直接是 Pi 的 Message 对象，需要验证解析逻辑是否兼容。---## 3. 实施清单### Phase 1: Rust 后端修复 (0.5 天)- [ ] `src-tauri/src/models.rs` 添加 `turns` 字段  ```rust  pub struct SubagentRunInfo {      // ... 现有字段      pub turns: usize,  // 新增  }  ```- [ ] `src-tauri/src/subagent.rs` 解析 `usage.turns`  ```rust  turns: usage.get("turns").and_then(|x| x.as_u64()).unwrap_or(0) as usize,  ```- [ ] `src-tauri/src/sqlite_cache/subagent_meta.rs` 更新缓存表  ```sql  ALTER TABLE subagent_meta_cache ADD COLUMN turns INTEGER DEFAULT 0;  -- 或者重建表  ```- [ ] 更新测试 `src-tauri/tests/subagent_cost_test.rs`### Phase 2: TypeScript 类型对齐 (0.5 天)- [ ] `src/types.ts` 添加缺失字段  ```typescript  export interface SubagentResult {    // ... 现有字段    agentId?: string      // 新增    outputFile?: string   // 新增    isSidechain?: boolean // 新增  }  ```- [ ] `src/types.ts` 添加 JSONL entry 类型  ```typescript  export interface SubagentOutputEntry {    isSidechain: boolean    agentId: string    type: 'user' | 'assistant' | 'toolResult'    message: Message    timestamp: string    cwd: string  }  ```### Phase 3: 解析器兼容性验证 (0.5 天)- [ ] 创建测试数据：使用 @tintinweb 格式生成 sample .output 和 .meta.json- [ ] 验证 `parse_meta_json` 能正确解析- [ ] 验证 `loadSubagentEntries` 能正确加载 JSONL- [ ] 验证 `SubagentModal` 能正确渲染### Phase 4: UI 细节优化 (0.5 天)- [ ] 在 `SubagentToolCall` 中显示 `turns` 信息- [ ] 在 `SubagentModal` 中显示 `outputFile` 路径（如果存在）- [ ] 确保 `agentId` 在 UI 中可见（用于调试）---## 4. 测试数据样本### 4.1 测试用 .meta.json```json{  "runId": "test-abc123",  "agent": "Explore",  "model": "anthropic/claude-sonnet-4",  "exitCode": 0,  "usage": {    "input": 1234,    "output": 5678,    "cacheRead": 0,    "cacheWrite": 0,    "cost": 0.015,    "turns": 5  },  "durationMs": 4500,  "toolCount": 3,  "timestamp": 1711450800000}```### 4.2 测试用 .output (JSONL)```jsonl{"isSidechain":true,"agentId":"test-abc123","type":"user","message":{"role":"user","content":"Find all files that handle authentication"},"timestamp":"2026-03-26T10:00:00.000Z","cwd":"/Users/test/project"}{"isSidechain":true,"agentId":"test-abc123","type":"assistant","message":{"role":"assistant","content":"I'll search for authentication-related files.","toolCalls":[{"id":"call_1","name":"find","arguments":{"pattern":"*auth*"}}]},"timestamp":"2026-03-26T10:00:05.000Z","cwd":"/Users/test/project"}{"isSidechain":true,"agentId":"test-abc123","type":"toolResult","message":{"role":"toolResult","toolCallId":"call_1","content":"Found: src/auth.ts, src/middleware/auth.ts"},"timestamp":"2026-03-26T10:00:10.000Z","cwd":"/Users/test/project"}```---## 5. 验收标准### 后端验收- [ ] `parse_meta_json` 单元测试通过（包含 turns 字段）- [ ] SQLite 缓存表结构更新完成- [ ] `scan_subagent_artifacts` 能正确聚合 turns 统计### 前端验收- [ ] TypeScript 编译无错误- [ ] SubagentToolCall 显示 turns 信息- [ ] SubagentModal 能加载并渲染 @tintinweb 格式的 JSONL- [ ] 点击结果卡片能正常打开详情弹窗### 集成验收- [ ] 使用 @tintinweb/pi-subagents 实际运行一个子代理- [ ] 项目能扫描并显示该子代理记录- [ ] 所有统计数字准确（tokens/cost/turns/tools）---## 6. 时间估算| Phase | 工作内容 | 估算时间 ||-------|---------|---------|| Phase 1 | Rust 后端修复 | 0.5 天 || Phase 2 | TypeScript 类型对齐 | 0.5 天 || Phase 3 | 解析器兼容性验证 | 0.5 天 || Phase 4 | UI 细节优化 | 0.5 天 || **总计** | | **2 天** |---## 7. 相关文件清单### 需要修改的文件```src-tauri/src/models.rs                      # 添加 turns 字段src-tauri/src/subagent.rs                    # 解析 usage.turnssrc-tauri/src/sqlite_cache/subagent_meta.rs  # 更新缓存表src/types.ts                                 # 添加 agentId/outputFile 等src/components/SubagentToolCall.tsx          # 显示 turnssrc-tauri/tests/subagent_cost_test.rs        # 更新测试```### 需要创建的文件```src-tauri/tests/tintinweb_compat_test.rs     # 兼容性测试scripts/test-tintinweb-compat.mjs            # 前端兼容性测试```---## 8. 下一步**立即执行：**1. 修改 `src-tauri/src/models.rs` 添加 `turns` 字段2. 修改 `src-tauri/src/subagent.rs` 解析 `usage.turns`3. 运行现有测试确保不破坏**然后：**4. 更新 TypeScript 类型5. 创建测试数据验证兼容性6. UI 细节优化---> 「塵も積もれば山となる」—— 积尘成山，小改动积累成大兼容让我们从 Phase 1 开始，用最小的改动实现兼容。