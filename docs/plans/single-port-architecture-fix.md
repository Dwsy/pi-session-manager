# 单端口架构修复计划## 问题当前 GUI 模式使用双端口：- WebSocket: `ws://127.0.0.1:52130` (独立端口)- HTTP API: `http://127.0.0.1:52131`CLI 模式使用单端口：- HTTP API + WebSocket(`/ws`): `http://127.0.0.1:52131` / `ws://127.0.0.1:52131/ws`**目标：统一 GUI 和 CLI 为单端口架构**---## 修复范围### 1. 后端配置默认值**文件**: `src-tauri/src/commands/settings.rs`**改动**:```rust// 当前 (双端口)impl Default for ServerSettings {    fn default() -> Self {        Self {            ws_enabled: true,            ws_port: 52130,      // ❌ 独立端口            http_enabled: true,            http_port: 52131,            auth_enabled: true,            bind_addr: default_bind_addr(),        }    }}// 修复后 (单端口)impl Default for ServerSettings {    fn default() -> Self {        Self {            ws_enabled: true,            ws_port: 52131,      // ✅ 与 HTTP 同端口            http_enabled: true,            http_port: 52131,            auth_enabled: true,            bind_addr: default_bind_addr(),        }    }}```---### 2. GUI 模式 WebSocket 初始化**文件**: `src-tauri/src/main.rs`**当前逻辑** (行 ~280-300):```rust// GUI 模式：启动独立 WS 服务if server_cfg.ws_enabled && !cli_mode {    let ws_state = app_state.clone();    let ws_port = server_cfg.ws_port;    let ws_bind = server_cfg.bind_addr.clone();    tauri::async_runtime::spawn(async move {        if let Err(e) =            pi_session_manager::ws_adapter::init_ws_adapter(ws_state, &ws_bind, ws_port).await        {            eprintln!("Failed to init WebSocket adapter: {e}");        }    });}```**修复方案**:- GUI 模式不再启动独立 WS 服务- 依赖 HTTP adapter 的 `/ws` 路径提供 WS 服务- 需要检查 `http_adapter` 是否已支持 `/ws` 路径---### 3. HTTP Adapter 验证**文件**: `src-tauri/src/http_adapter_impl.rs`**需要验证**:- [ ] 是否已注册 `/ws` WebSocket 路由- [ ] GUI 模式下是否正确启用 `/ws` 路由---### 4. 前端配置默认值**文件**: `src/components/settings/sections/APITestSettings.tsx`, `AdvancedSettings.tsx`, `Onboarding.tsx`**改动**:```typescript// 当前const DEFAULT_WS_PORT = 52130const DEFAULT_HTTP_PORT = 52131// 修复后const DEFAULT_WS_PORT = 52131  // 与 HTTP 同端口const DEFAULT_HTTP_PORT = 52131```---### 5. 文档更新**需要更新的文件**:- [ ] `README.md`- [ ] `README.zh.md`- [ ] `AGENTS.md`- [ ] `website/content/docs/configuration.mdx`- [ ] `website/content/docs/cli.mdx`- [ ] `website/content/docs/installation.mdx`- [ ] `CHANGELOG.md` (记录此变更)---## 实施步骤### Phase 1: 后端修复1. 修改 `ServerSettings::default()` 默认端口2. 修改 GUI 模式 WS 初始化逻辑（跳过独立 WS 服务）3. 验证 HTTP adapter 的 `/ws` 路由### Phase 2: 前端修复1. 更新所有默认端口配置2. 更新设置页面 UI 说明文本### Phase 3: 文档更新1. 更新所有相关文档2. 添加迁移指南（旧配置兼容性）### Phase 4: 测试验证1. GUI 模式测试2. CLI 模式测试3. 前端连接测试---## 兼容性考虑**旧配置迁移**:- 现有用户的 `server_settings` 可能已保存 `ws_port: 52130`- 首次启动时应检测并提示迁移- 或者在代码层面保持向后兼容（读取旧配置但建议使用新端口）---## 风险与回滚**风险**:- 现有自动化脚本可能硬编码了双端口- 文档示例可能需要批量更新**回滚方案**:- 保留 `ws_port` 配置字段，仅修改默认值- 用户可手动改回双端口模式---## 验收标准- [ ] GUI 模式默认使用单端口 (52131)- [ ] CLI 模式保持单端口 (52131)- [ ] 前端能正确连接 WS 和 HTTP- [ ] 所有文档端口描述一致- [ ] 现有测试通过