# Pi Config TUI > Status: PLANNING > Priority: P1 > Assignees: π-zero (coordinator), ZenIce (implementer) > Created: 2026-02-14 --- ## 1. `PiConfigSettings.tsx` ： user skills/prompts， extensions/themes/packages，，SaveFeature TODO。 ：** pi config TUI Architecture**，Implementation + Pi Settings， `pi config` Command。 ### | Pi File | | |---|---| | `settings-manager.js` | Config（global + project），deep merge， | | `package-manager.js` | npm/git ，， ResolvedPaths | | `config-selector.js` | TUI（、Search、toggle） | | `settings-selector.js` | Settings TUI（boolean/enum/submenu） | ### Design 1. **`+/-` **：`+path` ，`-path` ， settings.json 2. ****：origin（package/top-level）× scope（user/project）× resourceType（extensions/skills/prompts/themes） 3. ****：--，， 4. ****：global `~/.pi/agent/settings.json` + project `.pi/settings.json` --- ## 2. Analysis ### 2.1 Backend（Rust） | Command | State | Issue | |---|---|---| | `scan_skills` | ✅ | user skills， project/package | | `scan_prompts` | ✅ | user prompts， project/package | | `load_pi_settings` | ✅ | skills/prompts/extensions | | `save_pi_settings` | ✅ | ， | | `scan_extensions` | ❌ | — | | `scan_themes` | ❌ | — | | `scan_packages` | ❌ | — | ### 2.2 Frontend | Component | State | Issue | |---|---|---| | `PiConfigSettings.tsx` | ⚠️ | skills/prompts tab，，Save TODO | | `SettingsPanel.tsx` | ✅ | section ， | | `types.ts` | ⚠️ | SkillInfo/PromptInfo metadata | --- ## 3. ### T1: Backend — [ZenIce] **File：** `src-tauri/src/commands/skills.rs`（ `pi_resources.rs`） **：** 1. `ResourceMetadata` ： ```rust #[derive(Serialize, Deserialize, Clone, Debug)] pub struct ResourceMetadata { pub source: String, // "auto" | package name pub scope: String, // "user" | "project" pub origin: String, // "package" | "top-level" } #[derive(Serialize, Deserialize, Clone, Debug)] pub struct ResourceInfo { pub name: String, pub path: String, pub description: String, pub enabled: bool, pub resource_type: String, // "skills" | "extensions" | "prompts" | "themes" pub metadata: ResourceMetadata, } ``` 2. `scan_all_resources` Interface： - user skills: `~/.pi/agent/skills/` - project skills: `.pi/skills/`（ cwd Parameters） - user extensions: `~/.pi/agent/extensions/` - project extensions: `.pi/extensions/` - user prompts: `~/.pi/agent/prompts/` - project prompts: `.pi/prompts/` - user themes: `~/.pi/agent/themes/` - project themes: `.pi/themes/` - settings.json `+/-` enabled State - packages List， 3. `scan_skills` / `scan_prompts` Interface **：** - `scan_all_resources` `Vec<ResourceInfo>` - metadata（scope, origin, source） - enabled State settings.json `+/-` Config - `cargo test` ### T2: Backend — Pi Settings [ZenIce] **File：** `src-tauri/src/commands/skills.rs`（File `pi_settings.rs`） **：** 1. `load_pi_settings_full` settings.json ： ```rust #[derive(Serialize, Deserialize, Clone, Debug)] pub struct PiSettingsFull { pub default_provider: Option<String>, pub default_model: Option<String>, pub default_thinking_level: Option<String>, pub theme: Option<String>, pub steering_mode: Option<String>, pub follow_up_mode: Option<String>, pub hide_thinking_block: Option<bool>, pub quiet_startup: Option<bool>, pub collapse_changelog: Option<bool>, pub enable_skill_commands: Option<bool>, pub auto_compact: Option<bool>, pub shell_path: Option<String>, pub packages: Vec<serde_json::Value>, pub extensions: Vec<String>, pub skills: Vec<String>, pub prompts: Vec<String>, pub themes: Vec<String>, } ``` 2. `save_pi_setting` ： ```rust // File → → pub async fn save_pi_setting(key: String, value: serde_json::Value) -> Result<(), String> ``` 3. `toggle_resource` Interface： ```rust // settings.json +path -path pub async fn toggle_resource( resource_type: String, // "skills" | "extensions" | "prompts" | "themes" path: String, // Path enabled: bool, scope: String, // "user" | "project" ) -> Result<(), String> ``` **：** - `load_pi_settings_full` - `save_pi_setting` settings.json - `toggle_resource` `+/-` - project scope `.pi/settings.json` ### T3: Frontend — Type Transport [π-zero] **File：** `src/types.ts` **：** 1. TypeBackend： ```typescript export interface ResourceMetadata { source: string scope: 'user' | 'project' origin: 'package' | 'top-level' } export interface ResourceInfo { name: string path: string description: string enabled: boolean resourceType: 'skills' | 'extensions' | 'prompts' | 'themes' metadata: ResourceMetadata } export interface PiSettingsFull { defaultProvider?: string defaultModel?: string defaultThinkingLevel?: string theme?: string steeringMode?: string followUpMode?: string hideThinkingBlock?: boolean quietStartup?: boolean collapseChangelog?: boolean enableSkillCommands?: boolean autoCompact?: boolean shellPath?: string packages: unknown[] extensions: string[] skills: string[] prompts: string[] themes: string[] } ``` 2. Type ### T4: Frontend — PiConfigSettings [π-zero] **File：** `src/components/settings/sections/PiConfigSettings.tsx` **：** 1. Tab： - **Resources**（）： pi `config-selector` - **Settings**（Pi Settings）： pi `settings-selector` 2. Resources Tab： - `scan_all_resources` Data - scope → origin → resourceType - Search（name/path/type） - Toggle `toggle_resource` Save - ：`User (~/.pi/agent/)` / `Project (.pi/)` / `package-name (user)` 3. Settings Tab： - `load_pi_settings_full` Data - SettingsList，： - Boolean toggle（autoCompact, hideThinkingBlock, quietStartup ） - Enum select（steeringMode, followUpMode, defaultThinkingLevel） - Text input（shellPath, defaultProvider, defaultModel） - `save_pi_setting` Save **：** - Resources Display - Toggle settings.json - Settings `pi config` settings selector - Search - ### T5: Frontend — i18n [π-zero] **File：** `src/i18n/locales/zh-CN/settings.ts`, `src/i18n/locales/en-US/settings.ts` **：** Resources/Settings key ### T6: dispatch [ZenIce] **File：** `src-tauri/src/dispatch.rs`, `src-tauri/src/lib.rs`, `src-tauri/src/main.rs` **：** Command dispatch + Tauri IPC --- ## 4. ``` T1 (Backend) ──┐ ├──→ T4 (Frontend) ──→ T5 (i18n) T2 (BackendSettings) ──┤ └──→ T6 (dispatch ) T3 (FrontendType) ───────────→ T4 ``` ### | Agent | | | |---|---|---| | ZenIce | T1, T2, T6 | Backend Rust ， | | π-zero | T3, T4, T5 | Frontend React ，BackendInterface | **：** - ZenIce T1+T2，π-zero T3（Type） - T1+T2 Complete ZenIce T6，π-zero T4 - π-zero T5 --- ## 5. Interface ### 5.1 scan_all_resources ```json // Request { "command": "scan_all_resources", "payload": { "cwd": "/path/to/project" } } // Response { "success": true, "data": [ { "name": "ace-tool", "path": "skills/ace-tool/SKILL.md", "description": "Semantic search over codebase...", "enabled": true, "resource_type": "skills", "metadata": { "source": "auto", "scope": "user", "origin": "top-level" } }, { "name": "answer.ts", "path": "extensions/answer.ts", "description": "", "enabled": true, "resource_type": "extensions", "metadata": { "source": "auto", "scope": "user", "origin": "top-level" } } ] } ``` ### 5.2 load_pi_settings_full ```json // Request { "command": "load_pi_settings_full", "payload": {} } // Response { "success": true, "data": { "default_provider": "op4.6", "default_model": "claude-opus-4-6", "default_thinking_level": "xhigh", "theme": "light", "steering_mode": "one-at-a-time", "follow_up_mode": "one-at-a-time", "hide_thinking_block": false, "quiet_startup": false, "collapse_changelog": false, "enable_skill_commands": true, "auto_compact": true, "shell_path": null, "packages": ["npm:pi-context", "npm:pi-messenger"], "extensions": ["+extensions/answer.ts", "-extensions/custom-footer.ts"], "skills": [], "prompts": [], "themes": [] } } ``` ### 5.3 save_pi_setting ```json // Request { "command": "save_pi_setting", "payload": { "key": "hideThinkingBlock", "value": true } } // Response { "success": true } ``` ### 5.4 toggle_resource ```json // Request { "command": "toggle_resource", "payload": { "resource_type": "extensions", "path": "extensions/answer.ts", "enabled": false, "scope": "user" } } // Response { "success": true } ``` --- ## 6. 1. **settings.json **：pi settings.json，-- 2. **packages **：npm Path，V1 enable/disable State， 3. **project scope**：Project cwd，Tauri `std::env::current_dir()`，CLI ModeConfig 4. ****： `scan_skills` / `scan_prompts` Interface，Interface --- ## 7. - [ ] Resources tab Display user/project scope extensions/skills/prompts/themes - [ ] enabled State settings.json - [ ] Toggle settings.json， `+/-` - [ ] Settings tab Display pi Settings（provider, model, thinking, theme ） - [ ] SettingsSave， settings.json - [ ] Search - [ ] - [ ] `cargo test` TSC Error