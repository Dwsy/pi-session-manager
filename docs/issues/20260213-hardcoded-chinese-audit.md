# **Date**: 2026-02-13 **Status**: 🔍 Audit Complete **Priority**: P1 (High) ## SystemProject， i18n 。 ## ### 1. ErrorMessage (SettingsContext.tsx) **File**: `src/contexts/SettingsContext.tsx` ```typescript // Line 41 setError('LoadSettingsFailed') // Line 54 setError('SaveSettingsFailed') ``` **Fix**: ```typescript // i18n setError(t('settings.error.loadFailed', 'LoadSettingsFailed')) setError(t('settings.error.saveFailed', 'SaveSettingsFailed')) ``` --- ### 2. VerifyMessage (utils/settings.ts) **File**: `src/utils/settings.ts` ```typescript // Line 15 return { field: 'terminal.piCommandPath', message: 'Pi CommandPath' } // Line 21 return { field: 'session.refreshInterval', message: ' 5-300 ' } // Line 27 return { field: 'advanced.maxCacheSize', message: ' 10-1000 MB ' } // Line 33 return { field: 'appearance.sidebarWidth', message: ' 200-600 px ' } ``` **Fix**: ```typescript // t i18n return { field: 'terminal.piCommandPath', message: t('settings.validation.piCommandPathRequired', 'Pi CommandPath') } ``` --- ### 3. Display (utils/settings.ts) **File**: `src/utils/settings.ts` ```typescript // Line 86 return value ? '' : '' ``` **Fix**: ```typescript return value ? t('common.enabled', '') : t('common.disabled', '') ``` --- ### 4. SettingsDisplayName (utils/settings.ts) **File**: `src/utils/settings.ts` (Lines 178-214) **Issue**: `getSettingDisplayName()` ```typescript const displayNames: Record<string, Record<string, string>> = { terminal: { defaultTerminal: '', customTerminalCommand: 'Command', piCommandPath: 'Pi CommandPath', }, appearance: { theme: '', sidebarWidth: '', fontSize: '', codeBlockTheme: '', messageSpacing: 'Message', }, // ... } ``` **Fix**: ```typescript // A: i18n key function getSettingDisplayName(section: string, key: string): string { return t(`settings.${section}.${key}`, key) } // B: ， i18n // ， ``` --- ### 5. PluginNameDescription **File**: `src/plugins/session/SessionSearchPlugin.tsx` ```typescript // Lines 11-14 name = 'SessionSearch' description = 'SearchSessionNameData' keywords = ['session', 'file', 'conversation', 'Session', 'File', ''] ``` **File**: `src/plugins/project/ProjectSearchPlugin.tsx` ```typescript name = 'ProjectSearch' description = 'SearchProjectPath' ``` **File**: `src/plugins/message/MessageSearchPlugin.tsx` ```typescript name = 'MessageSearch' description = 'SearchUserMessage' ``` **Fix**: ```typescript // Plugin i18n class SessionSearchPlugin extends BaseSearchPlugin { get name() { return this.context?.t('plugins.session.name', 'SessionSearch') || 'SessionSearch' } get description() { return this.context?.t('plugins.session.description', 'SearchSessionNameData') || 'SearchSessionNameData' } keywords = ['session', 'file', 'conversation', 'Session', 'File', ''] } ``` --- ### 6. **File**: `src/plugins/session/SessionSearchPlugin.tsx` (Lines 114-138) ```typescript if (seconds < 60) return '' if (minutes < 60) return `${minutes} ` if (hours < 24) return `${hours} ` if (days < 7) return `${days} ` if (weeks < 4) return `${weeks} ` ``` **File**: `src/plugins/message/MessageSearchPlugin.tsx` (Lines 93-98) ```typescript if (days === 0) return '' if (days === 1) return '' if (days < 7) return `${days} ` if (days < 30) return `${Math.floor(days / 7)} ` if (days < 365) return `${Math.floor(days / 30)} ` return `${Math.floor(days / 365)} ` ``` **Fix**: ```typescript // i18n formatRelativeTime(date: Date): string { const seconds = Math.floor((Date.now() - date.getTime()) / 1000) if (seconds < 60) { return this.context.t('time.justNow', '') } const minutes = Math.floor(seconds / 60) if (minutes < 60) { return this.context.t('time.minutesAgo', '{{count}} ', { count: minutes }) } // ... } ``` --- ### 7. Message **File**: `src/plugins/session/SessionSearchPlugin.tsx` (Line 94) ```typescript defaultValue: `${session.message_count} Message` ``` **Fix**: ```typescript // t() defaultValue // defaultValue， i18n File description: context.t('session.messageCount', { count: session.message_count }) ``` --- ### 8. Project **File**: `src/components/ProjectFilterList.tsx` (Line 166) ```typescript if (!cwd || cwd === 'Unknown' || cwd === '') { // ... } ``` **Fix**: ```typescript // ，Display // const UNKNOWN_CWD = 'Unknown' if (!cwd || cwd === UNKNOWN_CWD) { // ... } ``` --- ## ### P0 - Fix（User） 1. ✅ **PluginNameDescription** - User 2. ✅ **** - Display 3. ✅ **ErrorMessage** - User ### P1 - （Feature） 4. **VerifyMessage** - Verify 5. **Display** - Settings ### P2 - （） 6. **SettingsDisplayName** - ， 7. **Project** - ， --- ## Fix ### Phase 1: Plugin i18n (P0) **File**: - `src/plugins/session/SessionSearchPlugin.tsx` - `src/plugins/project/ProjectSearchPlugin.tsx` - `src/plugins/message/MessageSearchPlugin.tsx` ****: ~2 ****: 1. Plugin i18n context 2. name/description getter 3. Implementation i18n 4. key locales --- ### Phase 2: ErrorVerifyMessage (P0-P1) **File**: - `src/contexts/SettingsContext.tsx` - `src/utils/settings.ts` ****: ~1 ****: 1. SettingsContext useTranslation 2. settings.ts Verify t Parameters 3. ErrorVerify key --- ### Phase 3: Optimize (P2) **File**: - `src/utils/settings.ts` (getSettingDisplayName) - `src/components/ProjectFilterList.tsx` ****: ~30 ****: 1. getSettingDisplayName 2. ， 3. Unknown --- ## Key ### i18n Keys ```typescript // common.ts export const common = { enabled: '', disabled: '', unknown: '', } // settings.ts () export const settings = { error: { loadFailed: 'LoadSettingsFailed', saveFailed: 'SaveSettingsFailed', }, validation: { piCommandPathRequired: 'Pi CommandPath', refreshIntervalRange: ' 5-300 ', cacheSizeRange: ' 10-1000 MB ', sidebarWidthRange: ' 200-600 px ', }, } // plugins.ts (File) export const plugins = { session: { name: 'SessionSearch', description: 'SearchSessionNameData', }, project: { name: 'ProjectSearch', description: 'SearchProjectPath', }, message: { name: 'MessageSearch', description: 'SearchUserMessage', }, } // time.ts (File) export const time = { justNow: '', minutesAgo: '{{count}} ', hoursAgo: '{{count}} ', daysAgo: '{{count}} ', weeksAgo: '{{count}} ', monthsAgo: '{{count}} ', yearsAgo: '{{count}} ', today: '', yesterday: '', } ``` --- ## ### 1. utils/settings.ts **Issue**: - `getSettingDisplayName()` - Verify i18n ****: - ， - Verify React Component， useTranslation ### 2. PluginArchitectureImprove **Issue**: - Plugin i18n context - name/description ****: - Plugin context ( t ) - name/description getter --- ## Verify FixCompleteVerify： - [ ] ErrorMessage - [ ] PluginNameDescription - [ ] - [ ] VerifyMessage - [ ] Update - [ ] （missing translation keys） --- ## File ### File 1. `src/contexts/SettingsContext.tsx` 2. `src/utils/settings.ts` 3. `src/plugins/session/SessionSearchPlugin.tsx` 4. `src/plugins/project/ProjectSearchPlugin.tsx` 5. `src/plugins/message/MessageSearchPlugin.tsx` 6. `src/plugins/base/BaseSearchPlugin.ts` 7. `src/components/ProjectFilterList.tsx` ### File 1. `src/i18n/locales/zh-CN/plugins.ts` 2. `src/i18n/locales/en-US/plugins.ts` 3. `src/i18n/locales/zh-CN/time.ts` 4. `src/i18n/locales/en-US/time.ts` 5. `src/i18n/locales/zh-CN/common.ts` () 6. `src/i18n/locales/en-US/common.ts` () --- ## ****: ~4 | Phase | | | |-------|--------|--------| | Phase 1: Plugin i18n | 2h | P0 | | Phase 2: Error/Verify | 1h | P0-P1 | | Phase 3: Optimize | 0.5h | P2 | | TestVerify | 0.5h | - | --- ****: Phase 1 - Plugin i18n