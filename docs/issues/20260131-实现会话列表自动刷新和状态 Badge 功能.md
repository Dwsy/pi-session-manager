--- id: "2026-01-31-ImplementationSessionListState Badge Feature" title: "ImplementationSessionListState Badge Feature" status: "in-progress" created: "2026-01-31" updated: "2026-01-31" category: "feature" tags: ["auto-refresh", "badge", "ux-improvement"] --- # Issue: ImplementationSessionListState Badge Feature ## Goal FrontendSessionList， Badge UpdateSession，User。 ## /Issue **State：** - ✅ Backend：， 30 Update SQLite - ❌ Frontend：， `Cmd+R` - ❌ Session，Update **User：** 1. SessionListUpdate， 2. SessionDisplay "NEW" badge 3. UpdateSessionDisplay "UPDATED" badge ## (Acceptance Criteria) - [ ] WHEN Session，Frontend SHALL 30-60 List - [ ] WHEN Session（），SHALL Display "NEW" badge（） - [ ] WHEN SessionContentUpdate（message_count ），SHALL Display "UPDATED" badge（） - [ ] WHEN UserSession，badge SHALL - [ ] WHERE User（Cmd+R），badge State SHALL Update ## ### Phase 1: - [x] AnalysisArchitecture（Backend，Frontend） - [x] Design（ + State） - [x] ### Phase 2: - [ ] Backend： `get_session_updates` Command，/UpdateSession ID - [ ] Backend： SQLite `first_seen_at` ， - [ ] Frontend：Implementation Hook（ 30 ） - [ ] Frontend：Implementation Badge Component（NEW/UPDATED） - [ ] Frontend： SessionList Badge - [ ] Frontend：Implementation Badge ### Phase 3: Verify - [ ] TestFeature - [ ] Test Badge Display - [ ] Test Badge - [ ] ### Phase 4: - [ ] Update - [ ] PR - [ ] ## | | | |------|------| | ~~ WebSocket~~ **FileSystem（notify）** | 、，FileUpdate， | | Badge State localStorage | Backend，FrontendState | | `first_seen_at` | SQLite `cached_at`，Update | | Badge | ，User | | Tauri SystemFrontend | BackendFile，Frontend， | ## ### 1. Backend #### 1.1 Data Schema Update ```sql ALTER TABLE sessions ADD COLUMN first_seen_at TEXT; ALTER TABLE sessions ADD COLUMN last_updated_at TEXT; ``` #### 1.2 Tauri Command ```rust #[tauri::command] pub async fn get_session_updates(last_check: String) -> Result<SessionUpdates, String> { // last_check UpdateSession } ``` ### 2. Frontend #### 2.1 Hook ```typescript function useAutoRefresh(interval: number = 30000) { useEffect(() => { const timer = setInterval(() => { loadSessions() }, interval) return () => clearInterval(timer) }, []) } ``` #### 2.2 Badge Component ```typescript function SessionBadge({ type }: { type: 'new' | 'updated' }) { return ( <span className={`badge ${type}`}> {type === 'new' ? 'NEW' : 'UPDATED'} </span> ) } ``` #### 2.3 Badge State ```typescript // localStorage: { sessionId: { viewed: boolean, firstSeen: timestamp } } const [badgeStates, setBadgeStates] = useState<Record<string, BadgeState>>({}) ``` ## Error | | Error | | |------|------|---------| | - | - | - | ## - [x] : `docs/architecture/storage-architecture.md`（） - [x] : `src-tauri/src/scanner_scheduler.rs` - [x] : `src/App.tsx` ## Notes **Implementation：** 1. ImplementationFrontend（，） 2. Implementation Badge Feature（Backend） **（Implementation）：** - Frontend： `setInterval` 30 `scan_sessions()` - Badge：Frontend sessions ，/Update - State： localStorage Session ID --- ## Status Update - **2026-01-31 15:20**: State → in-progress，: