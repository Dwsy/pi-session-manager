# Pi CLI RPC 后续开发计划

## 当前状态

RPC 基础架构已实现，包括：
- ✅ Rust 后端 RPC 客户端
- ✅ 前端 usePiRPC hook
- ✅ SessionViewer RPC 集成
- ✅ 自动检测和回退机制

## Phase 1: 实时消息流优化 (P0 - 1-2 周)

### 1.1 RPC 消息解析和显示
**目标**: 将 RPC 事件流转换为可显示的 SessionEntry

**任务**:
- [ ] 创建 `src/utils/rpcMessageParser.ts`
  - 将 `RPCEvent` 转换为 `SessionEntry`
  - 处理 `text_delta` 增量更新
  - 处理 `thinking_delta` 推理内容
  - 处理工具调用事件

- [ ] 修改 `SessionViewer` 支持 RPC 消息追加
  - 区分文件解析消息和 RPC 实时消息
  - 合并两种来源的消息
  - 处理消息重复问题

**技术要点**:
```typescript
// RPC 消息解析示例
function parseRPCToSessionEntry(event: RPCEvent): SessionEntry | null {
  if (event.type === 'message_update' && event.assistant_message_event) {
    // 处理增量文本
    const delta = event.assistant_message_event.delta
    // 追加到现有消息或创建新消息
  }
}
```

### 1.2 流式消息 UI 优化
**目标**: 更好的流式消息显示体验

**任务**:
- [ ] 创建 `StreamingMessage` 组件
  - 显示正在输入的光标动画
  - 支持思考内容折叠/展开
  - 实时字数统计

- [ ] 创建 `ThinkingBlock` 组件增强版
  - 支持流式思考内容
  - 思考时间统计
  - 代码高亮

**UI 设计**:
```
┌─────────────────────────────────────────┐
│ 🤖 Assistant                            │
│ ┌─────────────────────────────────────┐ │
│ │ 正在生成... ▌                        │ │
│ └─────────────────────────────────────┘ │
│ 💭 思考过程 (2.3s) [展开/折叠]          │
│ ┌─────────────────────────────────────┐ │
│ │ 让我分析一下这个问题...              │ │
│ │ 1. 首先考虑...                       │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 1.3 消息同步机制
**目标**: 确保 RPC 消息和文件内容同步

**任务**:
- [ ] 实现消息去重逻辑
  - 基于消息 ID 去重
  - 处理 RPC 和文件解析的重复消息

- [ ] 后台 SQLite 同步
  - RPC 消息实时写入 SQLite
  - 保持搜索功能可用

## Phase 2: 交互功能实现 (P1 - 2-3 周)

### 2.1 消息发送 UI
**目标**: 在 SessionViewer 中直接发送消息

**任务**:
- [ ] 创建 `MessageInput` 组件
  - 多行文本输入
  - 快捷键支持 (Enter 发送, Shift+Enter 换行)
  - 图片上传支持（可选）

- [ ] 集成到 SessionViewer
  - 底部输入框
  - 发送状态指示
  - 错误处理和重试

**界面布局**:
```
┌─────────────────────────────────────────┐
│ 消息列表区域                              │
│ ...                                     │
├─────────────────────────────────────────┤
│ [图片按钮] ┌──────────────────────┐ [发送]│
│           │ 输入消息...           │      │
│           │                      │      │
│           └──────────────────────┘      │
└─────────────────────────────────────────┘
```

### 2.2 干预和后续消息
**目标**: 支持 steer 和 followUp 功能

**任务**:
- [ ] 添加上下文菜单
  - 右键消息选择 "干预"
  - 快捷键 Ctrl+Shift+Enter

- [ ] 创建 `SteerDialog` 组件
  - 干预消息输入
  - 显示当前执行状态
  - 确认/取消按钮

### 2.3 模型切换 UI
**目标**: 在界面上切换 AI 模型

**任务**:
- [ ] 创建 `ModelSelector` 组件
  - 下拉选择模型
  - 显示当前模型信息
  - 模型能力标签（reasoning, vision 等）

- [ ] 集成到 SessionViewer Header
  - 点击展开模型列表
  - 实时切换反馈

## Phase 3: 工具执行可视化 (P2 - 2-3 周)

### 3.1 工具执行状态显示
**目标**: 实时显示工具执行过程

**任务**:
- [ ] 创建 `ToolExecutionCard` 组件
  - 工具名称和图标
  - 参数显示（可折叠）
  - 执行状态指示器
  - 部分结果实时显示
  - 最终结果展示

**状态流转**:
```
准备执行 → 执行中 [进度条] → 成功/失败
            ↓
         实时输出
```

### 3.2 支持的工具类型
**目标**: 可视化常见工具执行

**任务**:
- [ ] Bash 命令执行显示
  - 命令高亮
  - 实时输出流
  - 退出码显示

- [ ] 文件操作显示
  - 读取：显示文件内容预览
  - 写入：显示修改差异
  - 搜索：显示匹配结果

- [ ] Web 搜索显示
  - 搜索关键词
  - 结果列表
  - 来源链接

### 3.3 工具执行控制
**目标**: 支持中止和重试

**任务**:
- [ ] 添加中止按钮
  - 执行中显示中止按钮
  - 调用 `send_abort` 命令
  - 中止状态反馈

- [ ] 添加重试机制
  - 失败时显示重试按钮
  - 自动重试配置

## Phase 4: 高级功能 (P3 - 3-4 周)

### 4.1 会话管理增强
**目标**: 完整的会话生命周期管理

**任务**:
- [ ] 新建会话
  - 创建空白会话
  - 从当前会话分支
  - 选择父会话

- [ ] 会话切换优化
  - 快速切换面板
  - 最近会话列表
  - 会话预览

- [ ] 会话压缩
  - 手动压缩触发
  - 压缩进度显示
  - 压缩前后对比

### 4.2 多会话支持
**目标**: 同时管理多个活跃会话

**任务**:
- [ ] 会话标签页
  - 类似浏览器的标签页
  - 未读消息指示
  - 快速切换

- [ ] 会话分屏
  - 左右/上下分屏
  - 同步滚动对比

### 4.3 统计和监控
**目标**: 实时显示会话统计

**任务**:
- [ ] Token 使用统计
  - 输入/输出 token 数
  - 缓存命中统计
  - 成本估算

- [ ] 性能监控
  - 响应时间
  - 首 token 时间
  - 生成速度 (tokens/s)

## Phase 5: 性能优化 (P4 - 持续)

### 5.1 前端优化
**目标**: 提升大消息列表性能

**任务**:
- [ ] 虚拟列表优化
  - 更精确的预估高度
  - 动态高度缓存
  - 滚动性能优化

- [ ] 消息渲染优化
  - 增量渲染
  - 防抖处理
  - Web Worker 解析

### 5.2 后端优化
**目标**: 提升 RPC 通信效率

**任务**:
- [ ] 事件批处理
  - 高频事件合并
  - 节流处理

- [ ] 连接池管理
  - 多会话连接复用
  - 连接健康检查

### 5.3 存储优化
**目标**: 优化 SQLite 写入性能

**任务**:
- [ ] 批量写入
  - RPC 消息批量插入
  - 异步写入队列

- [ ] 索引优化
  - 查询性能分析
  - 索引调优

## 技术债务和重构

### 代码质量
- [ ] 添加单元测试
  - RPC 类型测试
  - 消息解析测试
  - Hook 测试

- [ ] 添加集成测试
  - RPC 通信测试
  - 端到端测试

- [ ] 文档完善
  - API 文档
  - 架构文档更新
  - 使用指南

### 重构计划
- [ ] 提取通用组件
  - 消息渲染逻辑
  - 工具执行显示

- [ ] 状态管理优化
  - 考虑使用 Zustand/Redux
  - 跨组件状态共享

## 发布计划

### v0.2.0 - RPC 基础版
- Phase 1 完成
- 实时消息流
- 基础发送功能

### v0.3.0 - 交互增强版
- Phase 2 完成
- 完整消息发送
- 模型切换

### v0.4.0 - 工具可视化版
- Phase 3 完成
- 工具执行显示
- 中止控制

### v0.5.0 - 完整客户端版
- Phase 4 完成
- 多会话支持
- 完整功能对齐 Pi Island

## 资源需求

### 开发资源
- 1 名前端开发者（React/TypeScript）
- 1 名后端开发者（Rust）
- 1 名 UI/UX 设计师（可选）

### 时间估算
- Phase 1: 1-2 周
- Phase 2: 2-3 周
- Phase 3: 2-3 周
- Phase 4: 3-4 周
- **总计**: 8-12 周

### 依赖项
- Pi CLI RPC 协议稳定性
- Tauri 2.x 稳定性
- 测试数据准备

## 风险评估

### 技术风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Pi CLI RPC 协议变更 | 高 | 封装协议层，快速适配 |
| 大消息列表性能问题 | 中 | 虚拟列表优化，分页加载 |
| 消息同步冲突 | 中 | 去重逻辑，版本控制 |

### 进度风险
| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 需求变更 | 中 | 敏捷开发，快速迭代 |
| 资源不足 | 中 | 优先级管理，分阶段发布 |

## 成功指标

### 功能指标
- [ ] RPC 连接成功率 > 95%
- [ ] 消息延迟 < 100ms
- [ ] 支持同时管理 10+ 会话

### 性能指标
- [ ] 首屏加载 < 2s
- [ ] 消息列表滚动 60fps
- [ ] 内存占用 < 500MB

### 用户满意度
- [ ] 实时性满意度 > 4.5/5
- [ ] 功能完整性 > 4/5
- [ ] 稳定性满意度 > 4.5/5
