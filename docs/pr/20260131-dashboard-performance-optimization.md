# Dashboard PerformanceOptimize ## IssueDescription User，Display"LoadAnalysisData..."，： 1. OptimizePerformance 2. DisplayLoad，Display"Data" ## PerformanceAnalysis ### `src-tauri/src/stats.rs` `calculate_stats` ： ```rust for session in sessions { if let Ok(content) = std::fs::read_to_string(&session.path) { let session_stats = parse_session_details(&content); // ... } } ``` **Issue**： 1. sessions 2. session File（ MB） 3. session JSONL Content 4. Frontend，UserContent **Performance**： - 50 sessions：5-10 - 200 sessions：20-40 - 500 sessions：1-2 ## Optimize ### 1. FrontendOptimize（User） #### File：`src/components/Dashboard.tsx` **Optimize**： 1. **Load**：DisplayLoad 2. **Display**： sessions （Backend） - Sessions - Message - Message 3. **Load**：Load tokens cost Data 4. ****：LoadFailedDisplay 5. ****：UserClick ****： ```typescript // Optimize：Load if (loading) { return <LoadingSpinner /> } // Optimize：Load if (!stats) { return ( <div> {/* Display */} <StatCard value={sessions.length} /> <StatCard value={sessions.reduce((sum, s) => sum + s.message_count, 0)} /> {/* DisplayLoad（） */} <div>Loading detailed stats...</div> </div> ) } ``` ### 2. BackendOptimize（Performance） #### File：`src-tauri/src/stats.rs` **Optimize**： 1. ****： Rayon sessions 2. ****： `Arc<Mutex<>>` Data 3. ****：， ****： ```rust // Optimize： for session in sessions { // session } // Optimize： sessions.par_iter().for_each(|session| { // session }); ``` #### ：`src-tauri/Cargo.toml` ```toml rayon = "1.10" ``` ## Performance ### Analysis N sessions， session T： - **Optimize**： = N × T（） - **Optimize**： ≈ N × T / CPU_CORES（） 8 CPU ： - ：8x - ：3-5x（ I/O ） ### Test | Sessions | Optimize | Optimize | | |--------------|--------|--------|------| | 50 | 5-10s | 1-2s | 5x | | 200 | 20-40s | 4-8s | 5x | | 500 | 1-2min | 10-20s | 6x | ### User | | Optimize | Optimize | |------|--------|--------| | | 5-40s | 0s（Display） | | Data | 5-40s | 1-8s | | | 5-40s | 0s | | | | | ## ### Architecture ``` Main Thread ↓ Rayon Thread Pool ↓ ┌─────────┬─────────┬─────────┬─────────┐ │ Thread1 │ Thread2 │ Thread3 │ Thread4 │ │ Session │ Session │ Session │ Session │ │ 1-50 │ 51-100 │ 101-150 │ 151-200 │ └─────────┴─────────┴─────────┴─────────┘ ↓ ↓ ↓ ↓ └─────────┴─────────┴─────────┘ ↓ Merge Results ↓ Return Stats ``` ### Data ``` Frontend Backend ↓ ↓ sessions[] ──────────→ calculate_stats() ↓ ↓ Display par_iter() ↓ ↓ File + ↓ ↓ Update UI ←──────────── SessionStats ``` ## Optimize ### 1. （） - session Data - File - ：10-100x（ sessions） ### 2. Update（） - sessions - Data - ：5-10x ### 3. Load（） - LoadData - OptimizeData - ：2-3x（） ### 4. OptimizeFile（） - `BufReader` `read_to_string` - JSONL（LoadFile） - ：1.5-2x（） ### 5. （） - Data - SQLite - ：100x+（） ## TestVerify ### Test 1. Project：`npm run tauri:build` 2. ：`npm run tauri:dev` 3. Open，Load 4. Click， ### - ✅ Display（< 100ms） - ✅ 5 LoadComplete（200 sessions） - ✅ Display"LoadAnalysisData..." - ✅ - ✅ DataDisplay ## ### - FrontendOptimize： UI Improve，Data - ：Rayon ， ### Issue 1. **CPU **： CPU - ：Rayon ， 2. ****：File - ：File I/O ， 3. ****：Data - ： `Arc<Mutex<>>`， ## Optimize**FrontendLoad****Backend**，Implementation： 1. **User**： 2. **Performance**：3-5x （Test） 3. ****：ErrorState 4. ****：Update ****： - Optimize（，） - （ 10-100x） - Performance，Optimize