# Subagent Cost Feature - TestComplete ## ✅ TestState： **Test**: 2026-02-27 **Test**: Backend Rust ****: 15/15 (100%) --- ## 📊 Test ### Test (4 ) ``` ✅ parse_meta_json_full ✅ parse_meta_json_missing_fields_use_defaults ✅ parse_meta_json_invalid_returns_none ✅ aggregate_runs_totals ``` ### Test (8 ) ``` ✅ test_parse_single_meta_json ✅ test_aggregate_multiple_runs ✅ test_scan_subagent_artifacts ✅ test_subagent_file_modification ✅ test_full_subagent_scanning_integration ✅ test_empty_subagent_directory ✅ test_multiple_session_directories ✅ test_malformed_meta_json_graceful_handling ``` ### Test (3 ) ``` ✅ calculate_stats_from_inputs_fallback_counts_messages ✅ get_day_stats_groups_projects_by_path_and_populates_hourly_distribution ✅ get_day_stats_distinguishes_same_project_name_different_paths ``` --- ## 🎯 TestFeature | Feature | Test | State | |------|------|------| | JSON | // JSON | ✅ | | | agent/ model | ✅ | | Directory | Directory/Directory/Directory | ✅ | | File | | ✅ | | Error | File | ✅ | | | SessionStats subagent | ✅ | --- ## 📁 File ``` src-tauri/tests/subagent_cost_test.rs # Test（14KB, 8 Test） scripts/test-subagent-cost.mjs # Test docs/pr/SUBAGENT_COST_TESTS.md # Test ``` --- ## 🚀 Test ### ```bash node ./scripts/test-subagent-cost.mjs ``` ### ```bash # Test cargo test --package pi-session-manager --lib subagent::tests # Test（） cargo test --package pi-session-manager --test subagent_cost_test -- --nocapture # Test cargo test --package pi-session-manager --lib stats::tests ``` --- ## 💡 ？ Test， Dashboard Display，： ### 1. ```bash # FrontendBackend npm run build cargo build --release # ``` ### 2. subagent Data ```bash # meta.json File ls ~/.pi/agent/sessions/*/subagent-artifacts/*_meta.json # Data sqlite3 ~/.pi/agent/sessions/sessions.db "SELECT COUNT(*) FROM subagent_meta_cache;" ``` ### 3. Demo Mode demo mode，Data（ subagent） ### 4. Data subagent artifacts session Directory --- ## 📋 Verify - [x] Backend（subagent.rs, stats.rs） - [x] Frontend（Dashboard.tsx, TokenStats.tsx） - [x] Type（types.ts） - [x] Data（subagent_meta_cache） - [x] Test（4 ） - [x] Test（8 ） - [x] Test（3 ） - [x] Success（Rust + TypeScript） - [ ] ← **** - [ ] subagent Data ← **** --- ## 🎬 1. **** ```bash cd /Users/dengwenyu/Dev/AI/pi-session-manager npm run build && cargo build --release ``` 2. **** 3. ** Dashboard** - Display `incl. $X.XX subagents` - TokenStats Component "Subagent Usage" 4. **Data** ```bash # subagent ls -la ~/.pi/agent/sessions/*/subagent-artifacts/ # meta.json Content cat ~/.pi/agent/sessions/*/subagent-artifacts/*_meta.json | head -50 ``` --- ## 📚 - [Test](./SUBAGENT_COST_TESTS.md) - Test - [PR #9](https://github.com/Dwsy/pi-session-manager/pull/9) - FeatureImplementation --- > 「，Test」✨