--- id: "2026-02-14-extract-dispatch-bridge-cli" title: "Extract dispatch.rs — CLI " status: "merged" created: "2026-02-14" updated: "2026-02-14" category: "refactoring" tags: ["cli", "dispatch", "rust", "feature-flags", "refactoring"] --- # Extract dispatch.rs — CLI > `ws_adapter.rs` dispatch ， CLI GUI Command，。 ## (Why) `src-tauri-cli` Implementation HTTP/WS ， `ping` `scan_sessions` Command。Project `ws_adapter.rs` 58 Command dispatch ， Tauri `AppState` ，CLI 。 ： CLI （Frontend） 58 Command，Implementation。 ## Content (What) 1. `dispatch.rs` — Command（ Tauri ） 2. `#[cfg(feature = "gui")]` — Tauri Module 3. CLI — `src-tauri-cli` `pi-session-manager` crate `cli` feature 4. GUI — `ws_adapter.rs` GUI-only Command， `dispatch.rs` ## Issue - Issue（ CLI Feature） ## TestVerify (Test Result) - [x] GUI lib (`cargo check --lib --features gui`) - [x] GUI binary (`cargo check --bin pi-session-manager --features gui`) - [x] CLI lib (`cargo check --lib --no-default-features --features cli`) - [x] CLI binary (`cargo build --release` in src-tauri-cli) - [x] CLI Verify：`/health` ok，`scan_sessions` 2664 Session，`get_all_tags` 5 - [x] FrontendVerify：`http://127.0.0.1:52131/` index.html (200 OK) ## Type - [x] 🚀 Refactoring - [x] ✨ New Feature (CLI Command) ## FileList | File | Type | Description | |------|---------|------| | `src-tauri/src/dispatch.rs` | | dispatch，58 Command， extract | | `src-tauri/src/lib.rs` | | `pub mod dispatch`；`http_adapter`/`app_state`/`file_watcher`/`terminal`/`ws_adapter` `#[cfg(feature = "gui")]`；`run()` `#[cfg(feature = "gui")]` | | `src-tauri/src/ws_adapter.rs` | | extract 450 dispatch ， GUI-only override + `crate::dispatch::dispatch()` | | `src-tauri/src/commands/settings.rs` | | `save_session_paths_core()` ；Fix `use tracing::warn` | | `src-tauri/src/commands/mod.rs` | | `terminal` Module `toggle_devtools` `#[cfg(feature = "gui")]` | | `src-tauri/src/commands/*.rs` | | `#[tauri::command]` → `#[cfg_attr(feature = "gui", tauri::command)]` | | `src-tauri-cli/Cargo.toml` | | `pi-session-manager` (path, cli feature) | | `src-tauri-cli/src/main.rs` | | WS/HTTP `pi_session_manager::dispatch::dispatch()`，Frontend | ## ### 1. dispatch.rs `ws_adapter.rs` 450 inline dispatch Module。： - `extract_string` / `extract_optional_string` / `extract_usize` - `pub async fn dispatch(command, payload) -> Result<Value, String>` - GUI-only Command（terminal_*、open_session_*）Error - `save_session_paths` dispatch.rs `save_session_paths_core`（ file watcher） ### 2. Feature Flag Architecture ``` src-tauri features: ├── gui (default) — tauri, app_state, ws_adapter, http_adapter, terminal, file_watcher └── cli — Module + dispatch.rs ws_adapter.rs dispatch : GUI-only commands (terminal, save_session_paths with watcher) → 58 Command → crate::dispatch::dispatch() CLI dispatch : Command → pi_session_manager::dispatch::dispatch() ``` ### 3. CLI `src-tauri-cli` Implementation，： - LoadConfig (`~/.config/pi-session-manager.json`) - WS/HTTP - Frontend (rust-embed) - `pi_session_manager::dispatch::dispatch()` ## - [x] GUI Mode。CLI Mode 2 Command 58 Command，。 ## Performance - [x] dispatch Path，。 ## - [x] - `src-tauri-cli` `pi-session-manager = { path = "../src-tauri", features = ["cli"] }` — crate ## State - **:** 2026-02-14 - **CLI :** 14MB (Frontend) - **Command:** 58/58 (GUI-only CommandError)