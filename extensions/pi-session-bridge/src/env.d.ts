/**
 * Pi extension types are imported from the project's pinned
 * @earendil-works/pi-coding-agent package. Keep this file free of local API stubs
 * so type-checking catches upstream ExtensionAPI changes.
 */
