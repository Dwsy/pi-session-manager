/**
 * Extension tool renderers are registered by PSM plugins.
 * Kept as a no-op export for older internal imports during the migration window.
 */
export function registerExtensionToolPlugins(): void {}
